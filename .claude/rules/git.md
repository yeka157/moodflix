# Git Conventions

- **Commit messages:** Conventional Commits — `feat:`, `fix:`, `style:`, `refactor:`, `docs:`, `chore:`, `perf:`
- **Branch names:** `feature/`, `fix/`, `refactor/`
- **Multi-account note:** Personal repos (this one) use the `github-personal` SSH host and `yuliuskevin66@gmail.com` identity. Work repos under `Rancang Nusantara Teknologi/` use `github-work-rancang` (configured via `.gitconfig` `includeIf`).
- **GitHub CLI:** Must be logged in as `yeka157` for this repo. Check with `gh auth status`; switch with `gh auth switch --user yeka157`.
