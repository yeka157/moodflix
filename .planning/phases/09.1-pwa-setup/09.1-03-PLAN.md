---
phase: 09.1-pwa-setup
plan: 03
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - scripts/generate-pwa-icons.js
  - public/icon-192.png
  - public/icon-512.png
  - public/icon-maskable.png
  - public/apple-touch-icon.png
autonomous: false
requirements: []

must_haves:
  truths:
    - "PWA icons use the cinematic crimson & dark theme — bold enough to stand out at small sizes on any home screen wallpaper"
    - "Maskable icon subject fits within the safe zone (inner 80% circle)"
    - "Icon set includes 192x192, 512x512, and 512x512 maskable variants"
    - "Apple touch icon is 180x180 with correct dark background"
  artifacts:
    - path: "scripts/generate-pwa-icons.js"
      provides: "Node.js script using sharp to generate all PWA icon sizes from source SVG"
    - path: "public/icon-192.png"
      provides: "192x192 PWA icon with cinematic crimson/dark design"
    - path: "public/icon-512.png"
      provides: "512x512 PWA icon with cinematic crimson/dark design"
    - path: "public/icon-maskable.png"
      provides: "512x512 maskable icon with safe-zone-compliant subject"
    - path: "public/apple-touch-icon.png"
      provides: "180x180 Apple touch icon"
  key_links:
    - from: "app/manifest.ts"
      to: "public/icon-192.png"
      via: "icons array src reference"
      pattern: "/icon-192.png"
---

<objective>
Generate new cinematic PWA icons matching the Moodflix brand — bold crimson M on dark background, optimized for home screen visibility at small sizes. Includes 192x192, 512x512, 512x512 maskable, and 180x180 apple-touch-icon variants.

Purpose: Replace the existing basic icons with a cinematic, brand-aligned icon set that stands out on any home screen wallpaper, completing the visual identity of the installed PWA.

Output: New icon files in public/ and a reusable generation script.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-pwa-setup/09.1-RESEARCH.md
@.planning/phases/09.1-pwa-setup/09.1-01-SUMMARY.md
@components/brand/moodflix-icon.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create icon generation script and generate new cinematic PWA icons</name>
  <files>
    scripts/generate-pwa-icons.js
    public/icon-192.png
    public/icon-512.png
    public/icon-maskable.png
    public/apple-touch-icon.png
  </files>
  <action>
**Create `scripts/generate-pwa-icons.js`:**
- Node.js CJS script using sharp (already installed via Next.js)
- Generates all PWA icon variants from a programmatically created SVG source
- The SVG source should render the Moodflix "M" logo (referencing the existing `MoodflixIcon` SVG design from `components/brand/moodflix-icon.tsx`):
  - Dark background: `#09090b` (Moodflix bg color)
  - Crimson M: `#FB2C36` (brand crimson)
  - Film-strip perforations as design element (matching existing brand identity)
  - The M should be large and centered — filling ~70% of the canvas for standard icons
  - For maskable: M at ~60% of canvas centered (safe zone = inner 80% circle per Android spec)
  - Subtle crimson glow/gradient behind the M for cinematic feel (optional, Claude's discretion on whether it looks good at 192px)

**Generate sizes:**
1. `icon-192.png` — 192x192, standard icon (M at ~70% of canvas)
2. `icon-512.png` — 512x512, standard icon (M at ~70% of canvas)
3. `icon-maskable.png` — 512x512, maskable icon (M at ~60% of canvas, extra padding for safe zone)
4. `apple-touch-icon.png` — 180x180, same design as icon-192 (Apple requires exactly 180x180)

**Run the script:**
```bash
node scripts/generate-pwa-icons.js
```

**Important notes:**
- Follow the same pattern as the existing `scripts/generate-og-base.js` — CJS require, sharp, fs/promises
- The `scripts/` directory is already excluded from ESLint (globalIgnores)
- sharp is available via `@img/sharp-darwin-arm64` (already installed for Next.js image optimization)
- Overwrite existing icon files in public/
- Existing decisions from Phase 02: "Maskable icon safe zone: M rendered at 60% of canvas centered on 512px dark background" and "All icons use solid #0a0a0a background" — update to #09090b for consistency with manifest
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && ls -la public/icon-192.png public/icon-512.png public/icon-maskable.png public/apple-touch-icon.png && node -e "const sharp = require('sharp'); async function check() { const m = await sharp('public/icon-192.png').metadata(); console.log('192:', m.width, m.height); const b = await sharp('public/icon-512.png').metadata(); console.log('512:', b.width, b.height); const k = await sharp('public/icon-maskable.png').metadata(); console.log('mask:', k.width, k.height); const a = await sharp('public/apple-touch-icon.png').metadata(); console.log('apple:', a.width, a.height); } check()"</automated>
    <manual>Open the generated PNG files and verify they look cinematic — bold crimson M on dark background, clearly visible at small sizes</manual>
  </verify>
  <done>All four icon variants generated with correct dimensions, cinematic crimson/dark design matching Moodflix brand identity</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PWA icons and overall PWA setup</name>
  <files>public/icon-192.png, public/icon-512.png, public/icon-maskable.png</files>
  <action>
Human verification checkpoint. No automated work — review the complete PWA setup:
1. Service worker with precaching + runtime caching (TMDB images, API routes)
2. Offline fallback page at /~offline
3. Unified manifest (standalone, dark theme, scope /)
4. SerwistProvider registration in root layout
5. Connectivity toast (online/offline via Sonner)
6. Install prompt banner (Chrome/Android only)
7. New cinematic PWA icons (192, 512, maskable, apple-touch)

Steps to verify:
1. Run `npm run build && npm run start` (must test with production build — SW disabled in dev)
2. Open http://localhost:3000 in Chrome
3. Open DevTools > Application tab:
   - Manifest: Check "App Manifest" section shows correct name, icons, display: standalone, scope: /
   - Service Worker: Check "Service Workers" section shows registered SW
   - Cache Storage: After navigating a few pages, check "Cache Storage" shows tmdb-images and api-routes caches
4. Install test (Chrome): Look for install icon in address bar or check if install banner appears at bottom of authenticated pages
5. Offline test:
   - Navigate to /home (let it cache)
   - Go to DevTools > Network > check "Offline"
   - Try navigating to a new uncached page — should show /~offline fallback
   - Check that the offline toast appears ("You're offline")
   - Uncheck "Offline" — should show "Back online" toast
6. Icons: Open the generated icon files and verify bold crimson M on dark background, clearly visible at small sizes, maskable icon M within safe zone (inner 80% circle)
7. iOS meta tags: View page source — look for apple-mobile-web-app-capable and apple-mobile-web-app-status-bar-style meta tags
  </action>
  <verify>Human visually confirms PWA setup works end-to-end</verify>
  <done>User approves PWA icons, offline behavior, install prompt, and overall PWA functionality</done>
</task>

</tasks>

<verification>
1. All four icon files exist with correct dimensions (192x192, 512x512, 512x512, 180x180)
2. Icons use cinematic crimson/dark design matching Moodflix brand
3. Maskable icon subject within 80% safe zone
4. Generation script is reusable for future icon updates
5. Full PWA stack verified end-to-end: manifest + SW + offline + icons + install prompt
</verification>

<success_criteria>
- Icon files look cinematic and brand-aligned at small sizes
- Full PWA stack works end-to-end in production build
- Service worker registers, caches work, offline fallback serves
- Human approves visual quality and functional behavior
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-pwa-setup/09.1-03-SUMMARY.md`
</output>
