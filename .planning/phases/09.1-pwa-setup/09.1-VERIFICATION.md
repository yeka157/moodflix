---
phase: 09.1-pwa-setup
verified: 2026-03-02T23:30:00Z
status: human_needed
score: 12/12 must-haves verified
re_verification: false
human_verification:
  - test: "Open Chrome DevTools > Application > Service Workers and verify SW registers at /serwist/sw.js"
    expected: "Service worker shows as 'activated and running', scope is /"
    why_human: "Service worker registration only activates in production build, cannot be verified statically"
  - test: "In Chrome DevTools > Application > Cache Storage, navigate several pages then check cache names"
    expected: "Caches named 'tmdb-images' and 'api-routes' appear after visiting movie pages"
    why_human: "Runtime caching only populates on actual network requests in running app"
  - test: "In Chrome DevTools > Application > Manifest, check the PWA manifest fields"
    expected: "Name: Moodflix, display: standalone, scope: /, icons show 192x192, 512x512, and 512x512 maskable"
    why_human: "Manifest parsing and display in browser requires live runtime context"
  - test: "Simulate offline in DevTools > Network > Offline, then navigate to an uncached page"
    expected: "You're offline page renders with dark background and 'You&apos;re offline' heading"
    why_human: "Offline fallback routing requires service worker to be active and page to not be cached"
  - test: "Go offline and then back online while on an authenticated page"
    expected: "Offline toast ('You're offline') appears and persists; 'Back online' success toast appears and auto-dismisses after ~4s"
    why_human: "Toast behavior requires navigator.onLine events — cannot simulate in static analysis"
  - test: "Open icon files public/icon-192.png, public/icon-512.png, public/icon-maskable.png visually"
    expected: "Bold crimson M lettermark on dark #09090b background, film-strip perforations visible, clearly legible at small sizes"
    why_human: "Visual quality of brand icons requires human judgment — automation confirms dimensions only"
  - test: "On Android Chrome with a production build, check for install banner"
    expected: "Bottom banner slides up with 'Install Moodflix' text, Install button (crimson), and X dismiss; install action triggers native Chrome prompt"
    why_human: "beforeinstallprompt event only fires on Android Chrome meeting PWA installability criteria"
---

# Phase 09.1: PWA Setup Verification Report

**Phase Goal:** Make Moodflix installable as a Progressive Web App — web app manifest, service worker, offline shell, caching, and install experience so users can add it to their home screen and get an app-like experience.
**Verified:** 2026-03-02T23:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Service worker registers successfully in production build | ? HUMAN | `app/sw.ts` fully implemented with Serwist; `app/serwist/[path]/route.ts` exports GET via `createSerwistRoute` serving `/serwist/sw.js`; SW registration cannot be confirmed without running production build |
| 2  | Offline fallback page is served when navigating to an uncached page while offline | ? HUMAN | `app/~offline/page.tsx` exists with correct content; precached via `additionalPrecacheEntries`; `fallbacks.entries` in sw.ts maps document requests to `/~offline`; actual offline routing requires live SW |
| 3  | App shell (HTML, JS, CSS) is precached on first visit | ? HUMAN | `self.__SW_MANIFEST` passed to `precacheEntries` in sw.ts; `defaultCache` from `@serwist/turbopack/worker` spread into runtimeCaching; precaching only verifiable in running production build |
| 4  | TMDB images are cached with CacheFirst strategy and LRU eviction at 500 entries | ✓ VERIFIED | `sw.ts` lines 30-43: `CacheFirst`, `cacheName: "tmdb-images"`, `maxEntries: 500`, `maxAgeSeconds: 30*24*60*60`, matcher checks `image.tmdb.org` hostname |
| 5  | API routes use NetworkFirst with 5-minute cache fallback | ✓ VERIFIED | `sw.ts` lines 44-59: `NetworkFirst`, `cacheName: "api-routes"`, `maxEntries: 50`, `maxAgeSeconds: 5*60`, matcher checks `/api/movies` and `/api/ai` pathnames |
| 6  | Only one manifest source exists — no duplicate site.webmanifest | ✓ VERIFIED | `public/site.webmanifest` deleted (confirmed absent); `app/manifest.ts` is sole canonical source at `/manifest.webmanifest` |
| 7  | User sees a subtle toast when connectivity changes (offline warning persists, online success auto-dismisses) | ? HUMAN | `OfflineToast` component: `hasMounted` ref guard present, `toast.warning("You're offline", { duration: Infinity })` for offline, `toast.success("Back online")` for online — behavior requires live browser |
| 8  | User sees a bottom install banner on Chrome/Android when PWA criteria are met | ? HUMAN | `InstallPrompt` gates on `beforeinstallprompt` event; `canInstall` gating present; positioned `bottom-16 md:bottom-0`; Framer Motion slide-up with `useReducedMotion()` — requires Chrome/Android runtime |
| 9  | Connectivity toast does not fire on initial page load | ✓ VERIFIED | `OfflineToast` uses `useRef(false)` hasMounted guard: first `useEffect` run sets `hasMounted.current = true` and returns early, preventing any toast on mount |
| 10 | PWA icons use cinematic crimson & dark theme at correct sizes | ✓ VERIFIED | `icon-192.png: 192x192`, `icon-512.png: 512x512`, `icon-maskable.png: 512x512`, `apple-touch-icon.png: 180x180` — all confirmed via sharp metadata |
| 11 | Maskable icon subject fits within the safe zone (inner 80% circle) | ✓ VERIFIED | `generate-pwa-icons.js` sets `scale: 0.6` for maskable icon — 60% of canvas centered, within the Android 80% safe zone per spec |
| 12 | SerwistProvider wired via SwRegister client wrapper into root layout | ✓ VERIFIED | `components/pwa/sw-register.tsx`: "use client", wraps `SerwistProvider swUrl="/serwist/sw.js"`; `app/layout.tsx`: imports `SwRegister`, wraps body children at lines 81-85 |

**Score:** 12/12 truths have substantive implementation (7 verified statically, 5 require human/runtime confirmation)

---

### Required Artifacts

#### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/sw.ts` | Service worker with precaching and runtime caching | ✓ VERIFIED | 76 lines; Serwist class instantiated with `precacheEntries`, `skipWaiting`, `clientsClaim`, `navigationPreload`, TMDB CacheFirst, API NetworkFirst, `~offline` fallback, `addEventListeners()` |
| `app/serwist/[path]/route.ts` | Turbopack route handler compiling sw.js | ✓ VERIFIED | `createSerwistRoute` with git revision, `additionalPrecacheEntries: [{url: "/~offline"}]`, `swSrc: "app/sw.ts"`, exports `dynamic`, `dynamicParams`, `revalidate`, `generateStaticParams`, `GET` |
| `app/~offline/page.tsx` | Offline fallback page | ✓ VERIFIED | Dark-themed `bg-background text-foreground`; "You're offline" heading; cached library message; `Metadata` title: "Offline" |
| `app/manifest.ts` | Unified manifest with standalone display, scope /, dark theme | ✓ VERIFIED | `display: "standalone"`, `scope: "/"`, `theme_color: "#09090b"`, `background_color: "#09090b"`, `orientation: "any"`, icons array includes `/icon-192.png`, `/icon-512.png`, `/icon-maskable.png` |
| `components/pwa/sw-register.tsx` | Client wrapper around SerwistProvider | ✓ VERIFIED | "use client", imports `SerwistProvider` from `@serwist/turbopack/react`, renders with `swUrl="/serwist/sw.js"` |
| `app/layout.tsx` | Root layout with SwRegister, iOS meta tags | ✓ VERIFIED | `SwRegister` wraps children + `SpeedInsights` + `Toaster`; `apple-mobile-web-app-capable: "yes"` and `apple-mobile-web-app-status-bar-style: "black-translucent"` meta tags in `<head>` |

#### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `hooks/use-online-status.ts` | SSR-safe online status hook | ✓ VERIFIED | "use client"; `navigator.onLine` SSR guard (`typeof navigator !== "undefined"`); `online`/`offline` event listeners with cleanup; exports `useOnlineStatus` |
| `hooks/use-pwa-install.ts` | beforeinstallprompt capture hook | ✓ VERIFIED | "use client"; `BeforeInstallPromptEvent` interface; `e.preventDefault()` on event; exposes `canInstall: boolean` and `install(): Promise<void>`; exports `usePwaInstall` |
| `components/pwa/offline-toast.tsx` | Headless toast component | ✓ VERIFIED | "use client"; `useOnlineStatus()` called; `hasMounted` ref guard; correct sonner toast calls; returns `null` (headless) |
| `components/pwa/install-prompt.tsx` | Bottom install banner | ✓ VERIFIED | "use client"; `usePwaInstall()` called; `framer-motion` slide-up; `useReducedMotion()`; Install and dismiss buttons; `bottom-16 md:bottom-0` positioning |
| `app/(app)/layout.tsx` | App layout with OfflineToast and InstallPrompt | ✓ VERIFIED | Imports both at lines 7-8; renders `<OfflineToast />` and `<InstallPrompt />` at lines 38-39 |

#### Plan 03 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/generate-pwa-icons.js` | Icon generation script | ✓ VERIFIED | CJS; uses `sharp`; SVG source with `#09090b` bg and `#FB2C36` crimson M; film-strip perforations; generates all 4 variants |
| `public/icon-192.png` | 192x192 PWA icon | ✓ VERIFIED | File exists, 192x192px confirmed via sharp metadata, 3578 bytes |
| `public/icon-512.png` | 512x512 PWA icon | ✓ VERIFIED | File exists, 512x512px confirmed, 9461 bytes |
| `public/icon-maskable.png` | 512x512 maskable icon | ✓ VERIFIED | File exists, 512x512px confirmed, scale 0.6 for safe zone, 8232 bytes |
| `public/apple-touch-icon.png` | 180x180 Apple touch icon | ✓ VERIFIED | File exists, 180x180px confirmed, 3267 bytes; also declared in `app/layout.tsx` metadata icons.apple |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/layout.tsx` | `app/serwist/[path]/route.ts` | SwRegister → SerwistProvider swUrl="/serwist/sw.js" | ✓ WIRED | `SwRegister` imported in layout; renders `SerwistProvider swUrl="/serwist/sw.js"` which requests `/serwist/sw.js` served by the `[path]` route handler |
| `app/serwist/[path]/route.ts` | `app/sw.ts` | `swSrc: "app/sw.ts"` in createSerwistRoute | ✓ WIRED | Line 16: `swSrc: "app/sw.ts"` — Serwist compiles this file for the GET handler |
| `app/sw.ts` | `app/~offline/page.tsx` | fallbacks entry with `/~offline` URL | ✓ WIRED | `fallbacks.entries[0].url: "/~offline"` (line 66); also in route handler `additionalPrecacheEntries` |
| `components/pwa/offline-toast.tsx` | `hooks/use-online-status.ts` | `useOnlineStatus()` hook call | ✓ WIRED | Line 4: `import { useOnlineStatus } from "@/hooks/use-online-status"`; called on line 7 |
| `components/pwa/install-prompt.tsx` | `hooks/use-pwa-install.ts` | `usePwaInstall()` hook call | ✓ WIRED | Line 4: `import { usePwaInstall } from "@/hooks/use-pwa-install"`; called on line 8 |
| `app/(app)/layout.tsx` | `components/pwa/offline-toast.tsx` | OfflineToast rendered in layout | ✓ WIRED | Import line 7; rendered at line 38 |
| `app/(app)/layout.tsx` | `components/pwa/install-prompt.tsx` | InstallPrompt rendered in layout | ✓ WIRED | Import line 8; rendered at line 39 |
| `app/manifest.ts` | `public/icon-192.png` | icons array src reference | ✓ WIRED | `src: "/icon-192.png"` at line 22; file confirmed to exist at correct path |

---

### Requirements Coverage

No formal requirement IDs were assigned to this phase (inserted as urgent phase). Phase goal coverage assessed directly against the phase goal statement.

| Goal Element | Status | Evidence |
|---|---|---|
| Web app manifest | ✓ SATISFIED | `app/manifest.ts` with standalone, scope /, dark theme, icons array |
| Service worker | ✓ SATISFIED | `app/sw.ts` + `app/serwist/[path]/route.ts` Turbopack route handler |
| Offline shell | ✓ SATISFIED | `app/~offline/page.tsx` precached via additionalPrecacheEntries |
| Caching strategies | ✓ SATISFIED | TMDB CacheFirst 500 entries; API NetworkFirst 5min; app shell via defaultCache |
| Install experience | ✓ SATISFIED | `components/pwa/install-prompt.tsx` with beforeinstallprompt; `components/pwa/offline-toast.tsx` for connectivity feedback |
| Home screen addable | ? HUMAN | Requires manifest + SW active in production — statically verified all prerequisites are in place |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `components/pwa/offline-toast.tsx` | 26 | `return null` | ℹ️ Info | Intentional — headless component by design, not a stub |
| `components/pwa/install-prompt.tsx` | 12 | `return null` | ℹ️ Info | Intentional — conditional rendering when `!canInstall || dismissed`, not a stub |

No blocking anti-patterns found. Both `return null` occurrences are correct by design.

---

### Human Verification Required

Plan 03 included a mandatory `checkpoint:human-verify` gate (Task 2) which the SUMMARY states was approved by the user. The following items still require confirmation in a live environment:

#### 1. Service Worker Registration

**Test:** Run `npm run build && npm start`, open Chrome DevTools > Application > Service Workers
**Expected:** Service worker registered at scope `/`, status "activated and running"
**Why human:** SW only activates in production build runtime — static analysis confirms implementation but not runtime behavior

#### 2. Cache Storage Population

**Test:** After SW registration, navigate to movie pages; check DevTools > Application > Cache Storage
**Expected:** `tmdb-images` cache contains poster images; `api-routes` cache contains API responses
**Why human:** Runtime caching strategies only populate on actual network requests

#### 3. Manifest Parsing

**Test:** Chrome DevTools > Application > Manifest
**Expected:** All fields shown correctly — standalone display, scope /, three icons, dark theme_color
**Why human:** Manifest parsing requires browser runtime

#### 4. Offline Fallback Routing

**Test:** Navigate to `/home`, enable Offline in DevTools > Network, navigate to a new uncached route
**Expected:** `/~offline` page renders with dark background and correct messaging
**Why human:** Offline routing requires active service worker intercepting navigations

#### 5. Connectivity Toasts

**Test:** While on an authenticated page, toggle Offline on/off in DevTools > Network
**Expected:** "You're offline" warning toast persists; "Back online" success toast auto-dismisses; neither fires on fresh page load
**Why human:** `navigator.onLine` events require browser environment

#### 6. PWA Icon Visual Quality

**Test:** Open `public/icon-192.png` and `public/icon-maskable.png` in image viewer
**Expected:** Bold crimson M on dark background, film-strip perforations visible, readable at small sizes; maskable M clearly within center 80% circle
**Why human:** Visual brand quality requires human judgment — dimensions confirmed programmatically (192x192, 512x512, 180x180 all correct)

#### 7. Install Banner (Chrome/Android)

**Test:** Open production build on Android Chrome that meets PWA criteria
**Expected:** Bottom banner slides up with "Install Moodflix", Install button, and X dismiss; install triggers native Chrome dialog
**Why human:** `beforeinstallprompt` only fires on Chromium when PWA criteria met — no desktop simulation possible

---

### Infrastructure Notes

- `tsconfig.json` correctly excludes `app/sw.ts` from compilation to prevent webworker lib type conflicts with main app DOM types
- `esbuild@^0.27.3` installed as dev dependency (required peer for `@serwist/turbopack`)
- `serwist@^9.5.6` and `@serwist/turbopack@^9.5.6` present in `package.json`
- All 7 commits from the three plans verified in git log (2ec67e0, 7fde24e, 8e46140, baa8e8a, b3c2ab7 + docs commits)

---

_Verified: 2026-03-02T23:30:00Z_
_Verifier: Claude (gsd-verifier)_
