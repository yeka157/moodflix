---
phase: 03-polish-qa
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false

must_haves:
  truths:
    - "npm run build completes with zero TypeScript errors"
    - "npm run lint exits with zero warnings"
    - "User can navigate the app at 375px width without layout breakage"
    - "User can navigate the app at 768px width without layout breakage"
    - "User can navigate the app at 1280px width without layout breakage"
    - "Page transitions are visible on route change"
    - "Crimson progress bar appears at top during navigation"
    - "Active nav pill slides to correct link on route change"
  artifacts:
    - path: "app/(app)/template.tsx"
      provides: "Entry animation for all (app) routes"
    - path: "app/(app)/layout.tsx"
      provides: "Layout with NextTopLoader"
  key_links:
    - from: "npm run lint"
      to: "zero warnings"
      via: "ESLint with next/core-web-vitals + typescript configs"
      pattern: "0 warnings"
    - from: "npm run build"
      to: "zero TypeScript errors"
      via: "Next.js build with TypeScript compiler"
      pattern: "Compiled successfully"
---

<objective>
Run the final build quality gate and perform the visual human verification across all three test breakpoints. This plan closes Phase 3.

Purpose: POLSH-04 requires `npm run build` and `npm run lint` to pass with zero errors/warnings. The visual checkpoint confirms POLSH-01, POLSH-02, POLSH-03 are working as intended before the phase is marked complete.
Output: Confirmed clean build + human sign-off on visual quality.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-polish-qa/03-01-SUMMARY.md
@.planning/phases/03-polish-qa/03-02-SUMMARY.md
@.planning/phases/03-polish-qa/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build quality gate</name>
  <files></files>
  <action>
    Run both quality gate commands from /Users/kevin/Repository/moodflix:

    ```bash
    npm run lint
    npm run build
    ```

    **If lint has warnings:** Identify each warning, trace to the file and line, fix it, re-run lint until zero warnings.

    **If build has TypeScript errors:** Read the error output carefully, fix each error (likely type issues introduced by hero-banner.tsx refactor or navbar changes), re-run build until it passes.

    Common issues to watch for:
    - hero-banner.tsx: `variants` prop on `motion.div` must accept the variant object type — if TypeScript flags it, add explicit type annotation: `const containerVariants: Variants = { ... }` and import `Variants` from framer-motion
    - app-navbar.tsx: `layoutId={shouldReduceMotion ? undefined : "nav-active-pill"}` — TypeScript should accept `string | undefined` on the layoutId prop, but verify
    - nextjs-toploader: if TypeScript flags missing types, the package ships its own types; no @types install needed

    Only report "done" when both commands exit with zero errors and zero warnings.
  </action>
  <verify>
    - `npm run lint` exits with code 0 and outputs no warnings
    - `npm run build` exits with code 0 and outputs "Compiled successfully" or similar success message
  </verify>
  <done>
    Both `npm run lint` and `npm run build` pass with zero errors and zero warnings.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification across all breakpoints</name>
  <files></files>
  <action>
    Claude starts the dev server (`npm run dev` from /Users/kevin/Repository/moodflix) and waits for the user to perform the visual checks described in `how-to-verify`. Claude does not perform visual verification — it presents the checklist and waits for the user's response.
  </action>
  <verify>User confirms "approved" after completing the visual checks at 375px, 768px, and 1280px+.</verify>
  <done>User has verified page transitions, responsive layouts at all breakpoints, and hero entrance animation.</done>
  <what-built>
    Phase 3 is complete. All four work streams are done:
    - 03-01: Lint fixes (4 warnings resolved)
    - 03-02: Page transitions (template.tsx, NextTopLoader, navbar pill, hero stagger)
    - 03-03: Responsive fixes (movie card touch targets, grid audit)
    - Build gate passed (zero errors, zero warnings)
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev` from /Users/kevin/Repository/moodflix (http://localhost:3000)

    **Visual check 1 — Page transitions (any viewport):**
    1. Navigate to /home, /discover, /library in sequence
    2. Confirm: each page arrival has a subtle fade+lift entrance (content fades in while sliding up 8px)
    3. Confirm: a thin crimson (#FB2C36) bar appears at the top of the page during each navigation
    4. Confirm: the active nav link indicator (faint red pill/background) slides smoothly to the correct link on each navigation — not a hard jump
    5. Load /home and confirm the hero image: backdrop zooms in slightly from 1.05x scale, then text elements appear one by one (genres → title → description → button) with a stagger

    **Visual check 2 — Mobile (375px):**
    Open Chrome DevTools → Device toolbar → iPhone SE or type 375px width manually.
    1. Navbar: Moodflix "M" icon visible (not full wordmark), nav links visible, avatar visible — no overlap
    2. Hero banner on /home: full height, text readable, CTA button visible, no overflow
    3. Movie grid: 2 columns, poster images load correctly, cards are large enough
    4. Movie cards: Bookmark and CircleCheck buttons feel large enough to tap comfortably (44px)
    5. Movie detail modal: opens and displays correctly, scrollable, close button accessible
    6. Library page: cards laid out correctly, no truncation issues

    **Visual check 3 — Tablet (768px):**
    Set DevTools to 768px width.
    1. Navbar: full logo visible (MoodflixLogo wordmark), nav labels visible alongside icons
    2. Movie grid: 4 columns visible
    3. Hero: well-proportioned, text not too small

    **Visual check 4 — Desktop (1280px+):**
    Set DevTools to 1280px or use full desktop window.
    1. Movie grid: 6 columns visible
    2. Navbar: correct layout, active pill animates on click
    3. Hero: cinematic, fills the banner area

    **Reduced motion check (optional but recommended):**
    Chrome DevTools → Rendering tab → Emulate prefers-reduced-motion: reduce
    Confirm all transitions are instant (no fade, no slide, no stagger)
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 3, or describe any issues found for fixes.</resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
1. `npm run lint` — zero warnings
2. `npm run build` — zero TypeScript errors, successful compilation
3. Human confirmed visual check at 375px, 768px, 1280px
4. Human confirmed page transitions visible and functioning
5. Human confirmed hero cinematic entrance on /home
</verification>

<success_criteria>
All 5 ROADMAP Phase 3 success criteria are met:
1. User navigates between routes and sees smooth Framer Motion page transitions — CONFIRMED by human checkpoint
2. User accesses app from mobile (375px), tablet (768px), desktop (1280px+) and all layouts work — CONFIRMED by human checkpoint
3. User navigates via keyboard only and can reach all interactive elements with visible focus states — PRE-EXISTING (Radix UI focus management, existing focus-visible styles)
4. Screen reader announces all interactive elements correctly and contrast meets WCAG 2.1 AA — PRE-EXISTING (aria-labels confirmed, contrast verified as passing in research)
5. Build completes with zero TypeScript errors and lint passes with zero warnings — CONFIRMED by build quality gate task
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-qa/03-04-SUMMARY.md` using the summary template.
</output>
