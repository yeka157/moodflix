---
phase: 11-discovery-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/app-sidebar.tsx
  - components/layout/bottom-tab-bar.tsx
  - components/movies/movie-detail-page.tsx
  - components/movies/tv-detail-page.tsx
autonomous: true
requirements:
  - DISC-02
  - DISC-03

must_haves:
  truths:
    - "Sidebar navigation item linking to /discover reads 'Movies' on desktop"
    - "Bottom tab bar item linking to /discover reads 'Movies' on mobile"
    - "Movie detail page shows rating as 'X.X/10' when vote_count > 10"
    - "TV detail page shows rating as 'X.X/10' when vote_count > 10"
    - "Rating badge is not rendered when vote_count is 10 or fewer"
  artifacts:
    - path: "components/layout/app-sidebar.tsx"
      provides: "Movies label for /discover nav item"
      contains: 'label: "Movies"'
    - path: "components/layout/bottom-tab-bar.tsx"
      provides: "Movies label for /discover nav item on mobile"
      contains: 'label: "Movies"'
    - path: "components/movies/movie-detail-page.tsx"
      provides: "X.X/10 rating format with vote_count guard"
      contains: "/10"
    - path: "components/movies/tv-detail-page.tsx"
      provides: "X.X/10 rating format with vote_count guard"
      contains: "/10"
  key_links:
    - from: "components/movies/movie-detail-page.tsx"
      to: "TMDB vote_count field"
      via: "conditional render guard"
      pattern: "vote_count.*>\\s*10"
    - from: "components/movies/tv-detail-page.tsx"
      to: "TMDB vote_count field"
      via: "conditional render guard"
      pattern: "vote_count.*>\\s*10"
---

<objective>
Rename the sidebar "Discover" label to "Movies" and replace the star rating format with "X.X/10" on detail pages.

Purpose: Accurately label the /discover page as "Movies" and show TMDB ratings in a clear, universally understood numeric format — hiding ratings for entries with insufficient votes.
Output: Updated nav labels in 2 layout components + updated rating display in 2 detail page components.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-discovery-ux/11-RESEARCH.md
@components/layout/app-sidebar.tsx
@components/layout/bottom-tab-bar.tsx
@components/movies/movie-detail-page.tsx
@components/movies/tv-detail-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename sidebar and bottom tab bar "Discover" label to "Movies"</name>
  <files>components/layout/app-sidebar.tsx, components/layout/bottom-tab-bar.tsx</files>
  <action>
In `components/layout/app-sidebar.tsx`, find the `navLinks` array entry with `href: "/discover"` and change `label: "Discover"` to `label: "Movies"`.

In `components/layout/bottom-tab-bar.tsx`, find the `navLinks` array entry with `href: "/discover"` and change `label: "Discover"` to `label: "Movies"`.

No other changes — keep the route `/discover`, the icon (`Compass`), and all aria/tooltip attributes unchanged. Per user decision: label text only, no tooltip/aria changes.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && grep -n '"Movies"' components/layout/app-sidebar.tsx components/layout/bottom-tab-bar.tsx && ! grep -n '"Discover"' components/layout/app-sidebar.tsx components/layout/bottom-tab-bar.tsx</automated>
    <manual>Desktop sidebar shows "Movies" for the /discover link; mobile bottom tab bar shows "Movies"</manual>
  </verify>
  <done>Both desktop sidebar and mobile bottom tab bar display "Movies" instead of "Discover" for the /discover route. Route path is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace star rating with X.X/10 format on detail pages</name>
  <files>components/movies/movie-detail-page.tsx, components/movies/tv-detail-page.tsx</files>
  <action>
In `components/movies/movie-detail-page.tsx`, find the rating Badge that renders `{rating} ★`. Replace it with a conditional render that only shows when `(details.vote_count ?? 0) > 10`. Change the content from `{rating} ★` to `{rating}/10`. Keep the same Badge variant and className.

Before (approximate):
```tsx
<Badge variant="secondary" className="text-sm px-3 py-1">
  {rating} ★
</Badge>
```

After:
```tsx
{(details.vote_count ?? 0) > 10 && (
  <Badge variant="secondary" className="text-sm px-3 py-1">
    {rating}/10
  </Badge>
)}
```

Apply the same change in `components/movies/tv-detail-page.tsx` — find the equivalent `{rating} ★` Badge and apply the same conditional render with `details.vote_count` guard and `/10` format.

Note: `vote_count` exists on both `MovieDetails` and `TVDetails` types — no type changes needed. The existing `rating = details.vote_average?.toFixed(1)` computation remains unchanged.

Text-only format — no star icon alongside the number. Per user decision: "Replace existing star rating entirely with X.X/10 numeric format — no stars alongside."
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && grep -n '/10' components/movies/movie-detail-page.tsx components/movies/tv-detail-page.tsx && grep -n 'vote_count' components/movies/movie-detail-page.tsx components/movies/tv-detail-page.tsx && ! grep -n '★' components/movies/movie-detail-page.tsx components/movies/tv-detail-page.tsx && npm run build</automated>
    <manual>Movie detail page shows "7.8/10" badge; TV detail page shows "8.2/10" badge; entries with ≤10 votes show no rating badge</manual>
  </verify>
  <done>Both movie and TV detail pages display TMDB rating as "X.X/10" format. Rating badge is hidden when vote_count is 10 or fewer. No star symbol remains. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes — no TypeScript errors from the rating conditional or label changes
2. `npm run lint` passes
3. Desktop sidebar shows "Movies" for /discover link
4. Mobile bottom tab bar shows "Movies" for /discover link
5. Movie detail page rating renders as "X.X/10" with vote_count > 10 guard
6. TV detail page rating renders as "X.X/10" with vote_count > 10 guard
7. No star (★) symbol remains in either detail page component
</verification>

<success_criteria>
- Sidebar and bottom tab bar display "Movies" instead of "Discover"
- Both detail pages show rating as "X.X/10" only when vote_count > 10
- Build and lint pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-discovery-ux/11-01-SUMMARY.md`
</output>
