---
phase: 10-tv-watchlisting-watchlist-ux
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - components/movies/tv-detail-page.tsx
  - components/movies/movie-detail-page.tsx
  - components/movies/movie-detail-modal.tsx
autonomous: true
requirements: [TVWL-01, TVWL-02, TVWL-03, TVWL-04]

must_haves:
  truths:
    - "User can add a TV show to their library from the /tv/[id] detail page by clicking the bookmark icon"
    - "User can mark a TV show as watched from the /tv/[id] detail page by clicking the CircleCheck icon"
    - "User can like or dislike a TV show from the /tv/[id] detail page using ThumbsUp/ThumbsDown icons"
    - "User can remove a TV show from their library from the /tv/[id] detail page"
    - "TV detail page action bar is visually identical to the movie detail page action bar"
    - "Movie detail page and modal pass mediaType to useWatchlistCheck and mutation calls"
  artifacts:
    - path: "components/movies/tv-detail-page.tsx"
      provides: "Full watchlist action bar for TV shows"
      contains: "useWatchlistCheck.*\"tv\""
    - path: "components/movies/movie-detail-page.tsx"
      provides: "Movie detail page with explicit mediaType='movie' in watchlist calls"
      contains: "useWatchlistCheck.*\"movie\""
    - path: "components/movies/movie-detail-modal.tsx"
      provides: "Movie detail modal with mediaType-aware watchlist calls"
      contains: "useWatchlistCheck.*mediaType"
  key_links:
    - from: "components/movies/tv-detail-page.tsx"
      to: "hooks/use-watchlist.ts"
      via: "useWatchlistCheck(details.id, 'tv')"
      pattern: "useWatchlistCheck\\(details\\.id.*\"tv\""
    - from: "components/movies/tv-detail-page.tsx"
      to: "actions/watchlist.ts"
      via: "addMutation with mediaType: 'tv'"
      pattern: "mediaType.*:.*\"tv\""
---

<objective>
Wire the full watchlist action bar into the TV detail page so users can add, mark watched, like/dislike, and remove TV shows from their library — identical UX to the movie detail page. Also update existing movie detail page and modal to pass explicit mediaType for correctness.

Purpose: This is the core TV watchlisting feature. Without it, users cannot manage TV shows in their library. The action bar must feel identical to the movie page so the experience is consistent.

Output: TVDetailPageContent with full watchlist CRUD, MovieDetailPageContent and MovieDetailModal with explicit mediaType params.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tv-watchlisting-watchlist-ux/10-RESEARCH.md
@.planning/phases/10-tv-watchlisting-watchlist-ux/10-01-SUMMARY.md

Key files to read before starting:
@components/movies/tv-detail-page.tsx
@components/movies/movie-detail-page.tsx
@components/movies/movie-detail-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire full watchlist action bar into TVDetailPageContent</name>
  <files>
    components/movies/tv-detail-page.tsx
  </files>
  <action>
**Replace the "TV show tracking coming soon" placeholder** in `TVDetailPageContent` with a full fixed bottom action bar, matching `MovieDetailPageContent` exactly.

**Imports to add:**
```
import { useWatchlistCheck, useAddToWatchlist, useRemoveFromWatchlist, useUpdateWatchlistStatus, useRateWatchlistItem } from "@/hooks/use-watchlist";
import { Bookmark, CircleCheck, ThumbsUp, ThumbsDown, Loader2, Trash2 } from "lucide-react";
import { toast } from "sonner";
```

**Hook calls (use mediaType "tv"):**
```typescript
const { data: watchlistItem, isLoading: isCheckingWatchlist } = useWatchlistCheck(details.id, "tv");
const addMutation = useAddToWatchlist();
const removeMutation = useRemoveFromWatchlist();
const statusMutation = useUpdateWatchlistStatus();
const rateMutation = useRateWatchlistItem();
```

**Derived state:**
```typescript
const isInLibrary = !!watchlistItem;
const isWatched = watchlistItem?.status === "watched";
const currentRating = watchlistItem?.rating ?? null;
```

**Handler functions — replicate from MovieDetailPageContent with these key differences:**
1. All `addMutation.mutate()` calls must include `mediaType: "tv"`
2. Use `details.name` (not `details.title`) for the `title` field — TV shows use `name` in TMDB
3. Use `details.poster_path` as-is (same field name as movie)
4. The undo toast in `handleRemove` must include `mediaType: "tv"` in its `addMutation.mutate` call
5. All `removeMutation.mutate()` calls must include `mediaType: "tv"`
6. All `statusMutation.mutate()` calls must include `mediaType: "tv"`
7. All `rateMutation.mutate()` calls — check if they need mediaType (inspect the existing movie page)

**Action bar JSX — fixed bottom bar matching movie page exactly:**
- Position: `fixed bottom-16 md:bottom-0 left-0 right-0 md:left-[60px]` (clears sidebar + mobile tab bar)
- Background: dark with backdrop blur
- Buttons: Bookmark (add/remove), CircleCheck (mark watched), ThumbsUp, ThumbsDown
- ThumbsUp/ThumbsDown only visible when `isInLibrary`
- Remove button (Trash2 icon) only visible when `isInLibrary`
- Loading state: show Loader2 spinner while `isCheckingWatchlist`
- Toast messages: "Added to Library", "Removed from Library" (with undo), "Marked as watched", etc.
- Icon fill states: filled when active (Bookmark filled when in library, CircleCheck filled when watched, ThumbsUp/Down filled when rated)

**TV-specific metadata to add alongside the action bar (above the bar, in the detail content area):**
- Number of seasons (from `details.number_of_seasons`)
- Episode count (from `details.number_of_episodes`)
- Display these near the existing rating/genre area, e.g.: "3 Seasons - 24 Episodes"

**Do NOT remove existing TV-specific content** (seasons, created_by, status badge, etc.) — only add watchlist functionality.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build 2>&1 | tail -5</automated>
    <manual>Navigate to a TV show detail page (/tv/[id]) and verify the action bar appears with bookmark, watched, like/dislike buttons</manual>
  </verify>
  <done>
    - TVDetailPageContent has a full fixed bottom action bar identical to MovieDetailPageContent
    - useWatchlistCheck called with mediaType "tv"
    - All mutations pass mediaType "tv"
    - Undo toast on remove re-adds with mediaType "tv"
    - TV-specific metadata (seasons, episodes) displayed alongside existing content
    - No existing TV-specific content removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MovieDetailPageContent and MovieDetailModal to pass explicit mediaType</name>
  <files>
    components/movies/movie-detail-page.tsx
    components/movies/movie-detail-modal.tsx
  </files>
  <action>
**components/movies/movie-detail-page.tsx:**
- Update `useWatchlistCheck(details.id)` → `useWatchlistCheck(details.id, "movie")`
- Update all `removeMutation.mutate({ id, tmdbId })` calls to include `mediaType: "movie"`
- Update all `statusMutation.mutate(...)` calls to include `mediaType: "movie"` (if the hook now expects it from Plan 01)
- Verify all `addMutation.mutate(...)` calls include `mediaType: "movie"` (they should already from Phase 9 changes, but verify)

**components/movies/movie-detail-modal.tsx:**
- The modal has a `mediaType` prop already (added in Phase 5/7 for TV branching)
- Update `useWatchlistCheck(movie?.id ?? 0)` → `useWatchlistCheck(movie?.id ?? 0, mediaType ?? "movie")`
- Update all `removeMutation.mutate(...)` calls to include `mediaType: mediaType ?? "movie"`
- Update all `addMutation.mutate(...)` calls to include `mediaType: mediaType ?? "movie"` (if not already)
- Update all `statusMutation.mutate(...)` calls to include `mediaType: mediaType ?? "movie"` (if applicable)
- Ensure the modal correctly handles both movie and TV watchlist state via its existing `mediaType` prop

**Verify all undo toasts** in both components pass `mediaType` in their `addMutation.mutate()` calls.

IMPORTANT: Read the existing code first — many of these changes may already be partially in place from Phase 9. Only add what is missing.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
    <manual>Verify MovieDetailPageContent and MovieDetailModal pass explicit mediaType to all watchlist hooks and mutations</manual>
  </verify>
  <done>
    - MovieDetailPageContent passes `"movie"` to `useWatchlistCheck` and all mutation calls
    - MovieDetailModal passes its `mediaType` prop to `useWatchlistCheck` and all mutation calls
    - All undo toasts include `mediaType` in their re-add calls
    - `npm run build` and `npm run lint` pass clean
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. `npm run lint` passes with zero warnings
3. TVDetailPageContent has a fixed bottom action bar with bookmark, watched, like/dislike, remove buttons
4. MovieDetailPageContent and MovieDetailModal use explicit mediaType in all watchlist calls
5. All undo toasts include mediaType
</verification>

<success_criteria>
Users can fully manage TV shows in their library from the /tv/[id] detail page: add, mark watched, like/dislike, and remove. The action bar is visually identical to the movie detail page. Movie detail page and modal are updated to use explicit mediaType for correctness with the new media-type-aware hooks.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tv-watchlisting-watchlist-ux/10-02-SUMMARY.md`
</output>
