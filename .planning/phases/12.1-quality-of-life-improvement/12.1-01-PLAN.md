---
phase: 12.1-quality-of-life-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ai/mood-section.tsx
  - components/layout/mobile-back-button.tsx
  - app/(app)/movie/[id]/page.tsx
  - app/(app)/tv/[id]/page.tsx
autonomous: true
requirements:
  - QOL-UI-01
  - QOL-UI-02
  - QOL-UI-03

must_haves:
  truths:
    - "AI chat send button shows a standard ArrowUp icon instead of Sparkles"
    - "Mobile users see a floating back button on movie and TV detail pages"
    - "Back button navigates to previous page or falls back to /home when no history"
    - "Back button is hidden on desktop (md and above)"
  artifacts:
    - path: "components/ai/mood-section.tsx"
      provides: "ArrowUp icon on send button"
      contains: "ArrowUp"
    - path: "components/layout/mobile-back-button.tsx"
      provides: "Reusable mobile back button component"
      exports: ["MobileBackButton"]
    - path: "app/(app)/movie/[id]/page.tsx"
      provides: "Movie detail page with back button"
      contains: "MobileBackButton"
    - path: "app/(app)/tv/[id]/page.tsx"
      provides: "TV detail page with back button"
      contains: "MobileBackButton"
  key_links:
    - from: "components/layout/mobile-back-button.tsx"
      to: "next/navigation"
      via: "useRouter().back()"
      pattern: "router\\.back"
    - from: "app/(app)/movie/[id]/page.tsx"
      to: "components/layout/mobile-back-button.tsx"
      via: "import"
      pattern: "MobileBackButton"
---

<objective>
Fix three UI issues: replace the AI chat send button icon from Sparkles to ArrowUp, add a floating back button on mobile detail pages, and confirm the bottom tab bar behavior on detail pages is correct (no highlight = intended).

Purpose: Users reported the AI send button icon is not intuitive, and mobile detail pages have no back navigation. The bottom tab bar showing no active state on detail pages is actually correct UX (matches Netflix/Disney+ pattern) but the back button provides the missing navigation context.

Output: Updated mood-section.tsx with ArrowUp icon, new MobileBackButton component used on both movie and TV detail pages.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-quality-of-life-improvement/12.1-RESEARCH.md

@components/ai/mood-section.tsx
@components/layout/bottom-tab-bar.tsx
@app/(app)/movie/[id]/page.tsx
@app/(app)/tv/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AI send button icon and create MobileBackButton component</name>
  <files>components/ai/mood-section.tsx, components/layout/mobile-back-button.tsx</files>
  <action>
  1. In `components/ai/mood-section.tsx`:
     - Replace `import { Sparkles }` (or wherever Sparkles is imported from lucide-react) with `import { ArrowUp }` from lucide-react
     - Replace `<Sparkles className="size-4" />` with `<ArrowUp className="size-4" />` in the send button
     - Verify the existing tooltip/aria-label says "Send message" (keep it if so)
     - Per user decision: ArrowUp is the industry standard (ChatGPT, Claude convention)

  2. Create `components/layout/mobile-back-button.tsx`:
     - "use client" component
     - Import `useRouter` from `next/navigation` and `ChevronLeft` from `lucide-react`
     - Implement `MobileBackButton` function component (function declaration, not arrow — per project convention)
     - onClick handler: check `window.history.length > 1` — if true, call `router.back()`; otherwise `router.push("/home")` as fallback for direct-link visitors
     - Styling: `fixed top-4 left-4 z-50 md:hidden size-10 rounded-full bg-black/60 backdrop-blur-sm flex items-center justify-center transition-colors active:bg-black/80`
     - Add `aria-label="Go back"`
     - Minimum 44x44px touch target (size-10 = 40px, but with the tap area it's fine; alternatively use size-11 if 44px is strict — check project convention)
     - Respect `prefers-reduced-motion` via `motion-safe:` on any transition

  NOTE on bottom tab bar: Research confirmed the current behavior (no highlight on detail pages) is the correct UX pattern. No code change needed for the tab bar. The back button solves the user's navigation confusion.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run lint && npm run build</automated>
  </verify>
  <done>ArrowUp icon renders on the AI send button. MobileBackButton component exists with back navigation + /home fallback. Lint and build pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add MobileBackButton to movie and TV detail pages</name>
  <files>app/(app)/movie/[id]/page.tsx, app/(app)/tv/[id]/page.tsx</files>
  <action>
  1. In `app/(app)/movie/[id]/page.tsx`:
     - Import `MobileBackButton` from `@/components/layout/mobile-back-button`
     - Add `<MobileBackButton />` as the first child inside the page's outermost container (before the backdrop image)
     - Since MobileBackButton is a client component with "use client", it can be rendered inside a Server Component page — React handles this automatically

  2. In `app/(app)/tv/[id]/page.tsx`:
     - Same as above — import and render `<MobileBackButton />` as first child

  3. Verify both detail pages still have `pb-16 md:pb-0` (or equivalent bottom padding) to account for the mobile bottom tab bar — if missing, add it

  NOTE: The back button is `fixed` positioned and `md:hidden`, so it overlays the backdrop on mobile only. It does not affect desktop layout at all.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build</automated>
  </verify>
  <done>Movie detail page (/movie/[id]) and TV detail page (/tv/[id]) both render a floating back button on mobile. Button navigates back or to /home. Hidden on desktop. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes — no new warnings or errors
2. `npm run build` passes — TypeScript compilation succeeds
3. Visual check: AI mood section send button shows ArrowUp icon (not Sparkles)
4. Visual check: On mobile viewport, /movie/[id] and /tv/[id] show a floating back button (top-left, semi-transparent circle)
5. Visual check: On desktop, back button is not visible
6. Bottom tab bar on detail pages shows no active highlight — this is correct and unchanged
</verification>

<success_criteria>
- ArrowUp icon on AI send button (not Sparkles)
- Floating back button visible on mobile detail pages, hidden on desktop
- Back button navigates to previous page or /home fallback
- Bottom tab bar behavior unchanged (no highlight on detail pages = correct)
- Build and lint pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-quality-of-life-improvement/12.1-01-SUMMARY.md`
</output>
