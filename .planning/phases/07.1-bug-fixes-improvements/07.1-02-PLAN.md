---
phase: 07.1-bug-fixes-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/series/page.tsx
  - components/series/series-content.tsx
  - components/movies/tv-detail-page.tsx
autonomous: true
requirements: [BACKLOG-28, BACKLOG-29]

must_haves:
  truths:
    - "Series page shows curated category rows (Trending, Korean Drama, Chinese Drama, Top Rated) above the filter grid"
    - "Korean dramas and Chinese dramas are visually separated into distinct labeled rows"
    - "TV detail page (/tv/[id]) shows per-season episode breakdown (not just total counts)"
  artifacts:
    - path: "app/(app)/series/page.tsx"
      provides: "Series page with curated rows + filter grid"
      min_lines: 20
    - path: "components/series/series-content.tsx"
      provides: "Curated TV rows with link-based navigation"
      contains: "hrefPrefix"
    - path: "components/movies/tv-detail-page.tsx"
      provides: "TV detail page with season breakdown section"
      contains: "season"
  key_links:
    - from: "app/(app)/series/page.tsx"
      to: "components/series/series-content.tsx"
      via: "import and render"
      pattern: "SeriesContent"
    - from: "app/(app)/series/page.tsx"
      to: "components/series/series-grid-content.tsx"
      via: "import and render below curated rows"
      pattern: "SeriesGridContent"
---

<objective>
Restore curated K-drama/C-drama/TV rows on the series page (BACKLOG-28) and add season episode breakdown to the TV detail page (BACKLOG-29).

Purpose: Users need clear visual separation between Korean dramas, Chinese dramas, and regular TV shows. The TV detail page should show per-season episode counts, not just totals.
Output: Series page with curated rows above filter grid; TV detail page with season breakdown section.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-bug-fixes-improvements/07.1-RESEARCH.md
@app/(app)/series/page.tsx
@components/series/series-content.tsx
@components/series/series-grid-content.tsx
@components/movies/tv-detail-page.tsx
@components/movies/movie-detail-modal.tsx (reference for SeasonsSection pattern)
@lib/tmdb.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore curated TV rows above filter grid on series page</name>
  <files>app/(app)/series/page.tsx, components/series/series-content.tsx</files>
  <action>
    1. Update `components/series/series-content.tsx`:
       - CRITICAL: Phase 7 established link-based navigation for TV shows (not modal). Replace the `onMovieClick`/`setSelectedShow` modal pattern with `hrefPrefix="/tv/"` on `MovieRow` components.
       - Remove the `useState` for `selectedShow` and the `MovieDetailModal` at the bottom.
       - Remove the `SeriesHeroBanner` import and rendering (the hero adds too much visual weight above the filter grid).
       - Remove `airingToday` from props — keep only: `trending`, `korean`, `chinese`, `topRated`.
       - Each `MovieRow` should pass `hrefPrefix="/tv/"` instead of `onMovieClick`. Check if `MovieRow` supports `hrefPrefix` — if not, it supports passing `href` on individual cards via the movie card's href prop. Look at MovieRow's props interface. If MovieRow does not support hrefPrefix, add it: a new optional `hrefPrefix?: string` prop that gets forwarded to each `MovieCard` as `href={hrefPrefix + movie.id}`.
       - Remove `MovieDetailModal` import entirely.

    2. Update `app/(app)/series/page.tsx`:
       - Convert to an async Server Component that fetches TV data server-side using the TMDB functions from `lib/tmdb.ts`.
       - Import `SeriesContent` and `SeriesGridContent`.
       - Fetch trending, Korean drama, Chinese drama, and top-rated TV data using the existing TMDB functions: `fetchTVCategory("trending")`, `fetchTVCategory("korean_drama")`, `fetchTVCategory("chinese_drama")`, `fetchTVCategory("top_rated")`. Check `lib/tmdb.ts` for exact function names — they may be `getTrendingTV`, `getTopRatedTV`, `getKoreanDramas`, `getChineseDramas` etc.
       - Render layout: page title + description, then `<SeriesContent>` with fetched data, then a divider/heading "Browse All", then `<SeriesGridContent />`.
       - Wrap TMDB fetches in try/catch — if any fails, pass empty array for that category.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /series — verify curated rows (Trending, Korean Drama, Chinese Drama, Top Rated) appear above the filter grid, and clicking a show navigates to /tv/[id]</manual>
  </verify>
  <done>Series page shows 4 curated category rows above the existing filter grid; each row uses link navigation to /tv/[id]; Korean and Chinese dramas are visually separated</done>
</task>

<task type="auto">
  <name>Task 2: Add season episode breakdown to TV detail page</name>
  <files>components/movies/tv-detail-page.tsx</files>
  <action>
    The TV detail page (`/tv/[id]`) currently shows total season count and episode count as metadata pills, but lacks the per-season breakdown that exists in `movie-detail-modal.tsx` via `SeasonsSection`.

    1. In `components/movies/tv-detail-page.tsx`:
       - Add a Seasons section below the "Where to Watch" section (before the "TV show tracking coming soon" notice).
       - Import the `TVSeason` type from `@/types/tv` (or wherever it's defined — check `types/tv.ts`).
       - Extract seasons from `details.seasons`, filtering out specials (`season_number > 0`).
       - If seasons exist, render a section with heading "Seasons ({count})" showing each season with:
         - Season poster thumbnail (if available, use `TMDB_IMAGE_BASE/w92{poster_path}`, otherwise a placeholder)
         - Season name (e.g., "Season 1")
         - Episode count + air year (e.g., "8 Episodes | 2024")
         - Mark the latest season with a small "Latest" badge
       - For shows with >5 seasons, show the 5 most recent (reversed order, latest first) with a "Show all N seasons" expand toggle.
       - Reference the `SeasonsSection` implementation in `movie-detail-modal.tsx` (lines 137-226) as a pattern — replicate a similar layout adapted for the full-page context (can be slightly larger/more spacious than the modal version).
       - Use `useState` for the expand/collapse toggle.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /tv/1396 (Breaking Bad) — verify per-season episode breakdown appears with season names, episode counts, and air years</manual>
  </verify>
  <done>TV detail page shows per-season episode breakdown with poster thumbnails, episode counts, air years, "Latest" badge on most recent season, and expand/collapse for 5+ seasons</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run lint` passes
- `npm run build` succeeds
- Series page renders curated rows + filter grid
- TV detail page shows season breakdown
</verification>

<success_criteria>
- Series page has curated rows (Trending, Korean Drama, Chinese Drama, Top Rated) above the filter grid
- Clicking any show in curated rows navigates to /tv/[id] (not modal)
- TV detail page shows per-season episode breakdown below watch providers
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-bug-fixes-improvements/07.1-02-SUMMARY.md`
</output>
