# Plan 07.1-02 Summary: Series Curated Rows & TV Detail Episodes

## What was done
1. **Curated TV rows**: Converted `SeriesContent` from client component with modal to Server Component with link-based navigation. Removed `"use client"`, `useState`, `MovieDetailModal`, `SeriesHeroBanner`, and `airingToday` prop. Added `hrefPrefix="/tv/"` and `mediaType="tv"` to all MovieRow components
2. **Series page SSR**: Converted `series/page.tsx` to async Server Component that fetches Trending, Korean Drama, Chinese Drama, and Top Rated TV data via `Promise.all`, normalizes through `normalizeTVShow()`, and passes to `SeriesContent` above a "Browse All" divider + `SeriesGridContent`
3. **TV detail seasons**: Added `SeasonsSection` component to `tv-detail-page.tsx` showing per-season breakdown with poster thumbnails, episode counts, air years, "Latest" badge, and expand/collapse for 5+ seasons

## Files modified
- `app/(app)/series/page.tsx` — async Server Component with data fetching
- `components/series/series-content.tsx` — simplified Server Component with link navigation
- `components/movies/tv-detail-page.tsx` — SeasonsSection component

## Decisions
- Removed `airingToday` row per plan (too much visual weight with curated rows + hero removed)
- Seasons sorted latest-first with 5 shown by default, expand toggle for more
- Specials (season_number 0) filtered out of display

## Verification
- `npx tsc --noEmit` passes
- `npm run lint` passes
- `npm run build` succeeds
