---
phase: 05-tv-series-page-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/series/page.tsx
  - app/(app)/series/loading.tsx
  - components/series/series-content.tsx
  - components/series/series-hero-banner.tsx
  - components/movies/movie-card.tsx
  - components/movies/movie-detail-modal.tsx
  - components/layout/app-navbar.tsx
autonomous: true
requirements: [TV-01]

must_haves:
  truths:
    - "A 'Series' link appears in the top navbar and navigates to /series"
    - "The /series page displays four labeled rows: Trending TV Shows, Korean Drama, Chinese Drama, Top Rated Series"
    - "Each row has horizontal scroll with arrow navigation (MovieRow reuse)"
    - "A hero banner at top of /series shows a trending TV show backdrop"
    - "Clicking the hero banner opens the TV detail modal"
    - "TV show cards do NOT show watchlist add/remove buttons (read-only)"
    - "The /series route shows a loading skeleton while data loads"
  artifacts:
    - path: "app/(app)/series/page.tsx"
      provides: "Server Component with SSR fetch for 4 TV categories + metadata"
      contains: "getTrendingTV"
    - path: "app/(app)/series/loading.tsx"
      provides: "Route-level loading skeleton matching discover pattern"
      contains: "Skeleton"
    - path: "components/series/series-content.tsx"
      provides: "Client Component managing selected show state and 4 MovieRow instances"
      contains: "MovieRow"
    - path: "components/series/series-hero-banner.tsx"
      provides: "Clickable hero banner for TV show with onClick handler"
      contains: "onClick"
    - path: "components/layout/app-navbar.tsx"
      provides: "Navbar with Series link added between Discover and Library"
      contains: "Series"
  key_links:
    - from: "app/(app)/series/page.tsx"
      to: "lib/tmdb.ts"
      via: "SSR fetch calls"
      pattern: "getTrendingTV|getTopRatedTV|discoverKoreanDramas|discoverChineseDramas"
    - from: "components/series/series-content.tsx"
      to: "components/movies/movie-row.tsx"
      via: "MovieRow component with normalized Movie[] data"
      pattern: "MovieRow.*title.*movies"
    - from: "components/series/series-content.tsx"
      to: "components/movies/movie-detail-modal.tsx"
      via: "selectedShow state passed as movie prop with mediaType='tv'"
      pattern: "MovieDetailModal.*mediaType"
    - from: "components/movies/movie-card.tsx"
      to: "hooks/use-watchlist.ts"
      via: "readOnly prop gates watchlist hooks and buttons"
      pattern: "readOnly"
---

<objective>
Scaffold the /series page with hero banner, four curated TV content rows, loading skeleton, and navbar integration. Add readOnly prop to MovieCard and MovieDetailModal so TV shows render without watchlist buttons.

Purpose: Deliver the complete TV series browsing UI as a pure composition of existing movie components, fulfilling TV-01's page structure, navbar, skeleton, and read-only requirements.
Output: Working /series page accessible from navbar, with hero + 4 rows + loading skeleton. TV cards and modal suppress watchlist controls.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tv-series-data-layer/04-01-SUMMARY.md
@.planning/phases/04-tv-series-data-layer/04-02-SUMMARY.md
@.planning/phases/05-tv-series-page-modal/05-RESEARCH.md

@components/movies/hero-banner.tsx
@components/movies/movie-card.tsx
@components/movies/movie-row.tsx
@components/movies/movie-detail-modal.tsx
@components/layout/app-navbar.tsx
@app/(app)/discover/page.tsx
@app/(app)/discover/loading.tsx
@hooks/use-tv.ts
@types/tv.ts
@types/movie.ts
@lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add readOnly prop to MovieCard and MovieDetailModal</name>
  <files>components/movies/movie-card.tsx, components/movies/movie-detail-modal.tsx</files>
  <action>
**MovieCard (components/movies/movie-card.tsx):**
1. Add `readOnly?: boolean` to `MovieCardProps` interface.
2. Accept `readOnly = false` in the destructured props.
3. When `readOnly` is true:
   - Do NOT call `useWatchlistTmdbIds()`, `useAddToWatchlist()`, `useRemoveFromWatchlist()`, `useUpdateWatchlistStatus()` — instead, conditionally skip them. Since React hooks cannot be called conditionally, keep the hook calls but gate ALL the derived state and button rendering behind `!readOnly`. Specifically:
     - The `useWatchlistTmdbIds()` call stays (it's a shared cache query, lightweight).
     - The mutation hooks stay but will never fire.
     - Wrap the entire "Action icons" `<div>` (the `absolute top-2 right-2 z-10` container with bookmark and check buttons) in `{!readOnly && (...)}` so the buttons are completely hidden.
   - The hover overlay (title, year, rating, genres) remains visible — only watchlist action buttons are removed.
4. No other changes to MovieCard behavior or styling.

**MovieDetailModal (components/movies/movie-detail-modal.tsx):**
1. Add `readOnly?: boolean` and `mediaType?: "movie" | "tv"` to `MovieDetailModalProps` interface.
2. Accept `readOnly = false` and `mediaType = "movie"` in the destructured props.
3. When `readOnly` is true:
   - Wrap the entire "Library Actions" section (`<div className="space-y-3">` containing all watchlist buttons and like/dislike) in `{!readOnly && (...)}` so the section is completely removed from the modal.
   - The watchlist hooks (`useWatchlistCheck`, `useAddToWatchlist`, etc.) stay in the component (React rules), but their results are never rendered.
4. The `mediaType` prop is added now but the TV-specific branching (conditional useTVDetails hook, TV fields display) will be implemented in Plan 02 Task 1. For now, just add the prop to the interface and accept it — no other changes based on mediaType in this task.

**Why read-only per TV-01 requirement:** "TV shows do NOT have watchlist add/remove buttons in v0.3 (read-only discovery)" — REQUIREMENTS.md TV-01 acceptance criterion. The CONTEXT.md discussion notes mentioned watchlist buttons, but the deferred section explicitly states a schema migration phase must be added first, which was NOT done. Requirements govern.
  </action>
  <verify>
Run `npm run build` — zero TypeScript errors. Verify that `readOnly` prop is optional with default `false`, so existing movie card/modal usage is unaffected (no breaking changes to discover, home, library pages).
  </verify>
  <done>
MovieCard renders without watchlist action buttons when `readOnly={true}`. MovieDetailModal renders without library actions section when `readOnly={true}`. Both props default to `false`, preserving all existing behavior. `mediaType` prop added to modal interface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /series page, hero banner, content component, loading skeleton, and navbar link</name>
  <files>app/(app)/series/page.tsx, app/(app)/series/loading.tsx, components/series/series-content.tsx, components/series/series-hero-banner.tsx, components/layout/app-navbar.tsx</files>
  <action>
**Navbar (components/layout/app-navbar.tsx):**
1. Import `Tv` from `lucide-react`.
2. Add a "Series" entry to the `navLinks` array between Discover and Library:
   ```
   { href: "/series", label: "Series", icon: Tv, exact: false }
   ```
   The final order: Home, Discover, Series, Library.

**Series page (app/(app)/series/page.tsx):**
1. Create a Server Component following the exact pattern from `app/(app)/discover/page.tsx`.
2. Import `getTrendingTV`, `getTopRatedTV`, `discoverKoreanDramas`, `discoverChineseDramas` from `@/lib/tmdb`.
3. Import `normalizeTVShow` from `@/types/tv` and `SeriesContent` from `@/components/series/series-content`.
4. Export metadata: `title: "Series"`, `description: "Browse trending and top-rated TV shows, Korean dramas, and Chinese dramas."`.
5. Fetch all 4 categories in parallel with `Promise.all`.
6. Normalize results: `trending.results.map(normalizeTVShow)`, etc.
7. Pass all 4 normalized `Movie[]` arrays to `<SeriesContent>`.
8. Keep the JSX minimal — no extra wrapper divs around SeriesContent (hero banner needs direct access to `-mx-4 -mt-8` negative margin offset from the app layout's `container mx-auto px-4 py-8`).

**Series hero banner (components/series/series-hero-banner.tsx):**
1. Create a `"use client"` component adapting the pattern from `components/movies/hero-banner.tsx`.
2. Props: `show: Movie`, `onClick: () => void`.
3. Select the hero show's display data: year from `show.release_date?.slice(0, 4)`, genre names from `TV_GENRES` (import from `@/lib/constants`), rating.
4. Layout mirrors HeroBanner: `-mx-4 -mt-8 h-[50vh] min-h-[400px] max-h-[600px] overflow-hidden`, full backdrop image, gradient overlays (both bottom-up and left-right), content positioned at bottom-left.
5. Make the entire container clickable: add `onClick`, `role="button"`, `tabIndex={0}`, `onKeyDown` for Enter key. Add `aria-label={`View details for ${show.title}`}`.
6. CTA button: "More Info" with `Info` icon from lucide-react (not a `<Link>`, just a `<Button>` with the same onClick).
7. Include Framer Motion stagger animation matching HeroBanner: `containerVariants`/`itemVariants` with `useReducedMotion` gating.
8. Show genre badges, year badge, title, overview (line-clamp-3), and the "More Info" button.
9. Use `TV_GENRES` for genre badge resolution (not `GENRES` — TV genre IDs differ). For common IDs like 18 (Drama) that exist in both, `TV_GENRES` will still resolve them (Phase 4 added all common overlapping IDs too). Actually, check: `TV_GENRES` only has TV-exclusive IDs (10759+). For shared IDs, merge lookup: try `TV_GENRES[id]` first, fall back to `GENRES[id]`. Import both from `@/lib/constants`.

**Series content (components/series/series-content.tsx):**
1. Create a `"use client"` component.
2. Props interface: `trending: Movie[]`, `korean: Movie[]`, `chinese: Movie[]`, `topRated: Movie[]`.
3. State: `selectedShow: Movie | null` (initially null).
4. Featured show: `trending.find((t) => t.backdrop_path) ?? trending[0] ?? null` — first trending show with a backdrop (deterministic, no Math.random).
5. Render:
   - `<SeriesHeroBanner>` if `featuredShow` exists, with `onClick={() => setSelectedShow(featuredShow)}`
   - `<MovieRow title="Trending TV Shows" movies={trending} onMovieClick={setSelectedShow} />`
   - `<MovieRow title="Korean Drama" movies={korean} onMovieClick={setSelectedShow} />`
   - `<MovieRow title="Chinese Drama" movies={chinese} onMovieClick={setSelectedShow} />`
   - `<MovieRow title="Top Rated Series" movies={topRated} onMovieClick={setSelectedShow} />`
   - `<MovieDetailModal movie={selectedShow} onClose={() => setSelectedShow(null)} mediaType="tv" readOnly />`
6. Pass `readOnly` to MovieDetailModal (no watchlist buttons for TV in v0.3).
7. Note: MovieRow internally uses MovieCard. To make TV cards read-only, we need MovieRow to pass `readOnly` down to MovieCard. However, MovieRow currently does not accept a `readOnly` prop. We have two options:
   - Option A: Add `readOnly?: boolean` to `MovieRowProps` and pass it to `<MovieCard readOnly={readOnly}>`.
   - Option B: Since the `MovieCard` hover overlay (genre badges) uses `GENRES` which won't resolve TV-exclusive genre IDs (10759+), and the watchlist buttons would be the only functional issue (cross-ID contamination), we can accept that watchlist buttons show on TV cards but won't work correctly.

   **Go with Option A** — add `readOnly?: boolean` to MovieRow and pass it to MovieCard. This cleanly fulfills the TV-01 requirement ("TV shows do NOT have watchlist add/remove buttons"). Modify `components/movies/movie-row.tsx`: add `readOnly?: boolean` to `MovieRowProps`, pass `<MovieCard movie={movie} onClick={onMovieClick} readOnly={readOnly} />`.

**Loading skeleton (app/(app)/series/loading.tsx):**
1. Match discover page skeleton pattern.
2. Include hero skeleton at top: `<Skeleton className="-mx-4 -mt-8 h-[50vh] min-h-[400px] max-h-[600px] rounded-none" />`
3. Then 4 row skeletons: each with a title skeleton (`h-7 w-40`) and 8 poster card skeletons (`w-[185px] aspect-[2/3] rounded-lg`).
  </action>
  <verify>
1. `npm run build` passes with zero errors.
2. `npm run lint` passes with zero warnings.
3. Verify navbar shows 4 links: Home, Discover, Series, Library.
4. Verify `/series` page renders with hero banner + 4 labeled rows.
  </verify>
  <done>
/series page accessible via navbar "Series" link. Page displays clickable hero banner + four content rows (Trending TV Shows, Korean Drama, Chinese Drama, Top Rated Series) using MovieRow with readOnly cards. Loading skeleton displays during SSR fetch. Clicking any card or hero opens the TV detail modal (which still shows movie-style content in this task — TV-specific fields added in Plan 02).
  </done>
</task>

</tasks>

<verification>
1. Navigate to /series via navbar — page loads with hero banner and 4 rows
2. Loading skeleton appears briefly during navigation (or on hard refresh)
3. TV show cards have NO bookmark/check action buttons (readOnly)
4. Clicking a card opens the detail modal (movie-style for now)
5. Hero banner is keyboard-accessible (Tab + Enter opens modal)
6. Existing movie pages (/discover, /home, /library) are unaffected — buttons still work
7. `npm run build && npm run lint` both pass clean
</verification>

<success_criteria>
- /series page renders 4 labeled TV rows with horizontal scroll and arrow navigation
- Hero banner shows trending TV show backdrop, is clickable, opens modal
- TV cards have NO watchlist buttons (readOnly mode)
- Loading skeleton matches discover page pattern
- "Series" link appears in navbar between Discover and Library
- Zero build errors, zero lint warnings
- No regressions on existing movie pages
</success_criteria>

<output>
After completion, create `.planning/phases/05-tv-series-page-modal/05-01-SUMMARY.md`
</output>
