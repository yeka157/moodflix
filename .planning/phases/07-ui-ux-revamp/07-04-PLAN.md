---
phase: 07-ui-ux-revamp
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - components/movies/movie-card.tsx
  - components/movies/movie-row.tsx
  - components/movies/home-movies.tsx
  - components/movies/discover-grid-content.tsx
  - components/series/series-grid-content.tsx
  - components/watchlist/watchlist-content.tsx
  - components/ai/recommendations-grid.tsx
  - components/movies/movie-search-drawer.tsx
  - components/layout/app-navbar.tsx
autonomous: false
requirements: [REVAMP-01]

must_haves:
  truths:
    - "Clicking a movie card on home, discover grid, series grid, or library navigates to /movie/[id] or /tv/[id] detail page"
    - "Clicking a search result on Discover opens a compact drawer/sheet with 'View Full Details' link to the detail page"
    - "All existing features remain fully functional: home hero + trending + personalized rows, discover search + grid, series grid, library tabs, AI mood section"
    - "The old top navbar component is removed from the codebase"
    - "Responsive: sidebar collapses to icon-only on desktop hover, bottom tab bar on mobile, detail pages work at all breakpoints"
  artifacts:
    - path: "components/movies/movie-card.tsx"
      provides: "Updated card with href prop for link-based navigation"
      contains: "href"
    - path: "components/movies/movie-search-drawer.tsx"
      provides: "Compact search result drawer for Discover"
      contains: "Sheet"
  key_links:
    - from: "components/movies/movie-card.tsx"
      to: "app/(app)/movie/[id]/page.tsx"
      via: "Next.js Link href"
      pattern: "href.*movie"
    - from: "components/movies/movie-search-drawer.tsx"
      to: "app/(app)/movie/[id]/page.tsx"
      via: "View Full Details link"
      pattern: "View Full Details"
---

<objective>
Migrate all movie/TV card click handlers from modal-based to link-based navigation, create a compact search drawer for Discover, and remove the old navbar. Final integration and cleanup.

Purpose: Complete the navigation migration so all browsing contexts route to the new detail pages. The search drawer preserves quick-preview behavior for search results on Discover. Removing the old navbar completes the UI/UX revamp.
Output: Updated MovieCard with dual-mode (link/callback), search drawer component, cleaned-up codebase with old navbar removed.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-revamp/07-01-SUMMARY.md
@.planning/phases/07-ui-ux-revamp/07-02-SUMMARY.md
@.planning/phases/07-ui-ux-revamp/07-03-SUMMARY.md

Key existing files:
@components/movies/movie-card.tsx (current: onClick callback)
@components/movies/movie-row.tsx (passes onMovieClick to cards)
@components/movies/home-movies.tsx (manages modal state)
@components/movies/discover-grid-content.tsx (created in Plan 03)
@components/series/series-grid-content.tsx (created in Plan 03)
@components/watchlist/watchlist-content.tsx (library page)
@components/ai/recommendations-grid.tsx (AI mood results)
@components/movies/movie-detail-modal.tsx (existing modal — to be preserved for search drawer adaptation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add href prop to MovieCard, migrate all call sites to link-based navigation</name>
  <files>
    components/movies/movie-card.tsx
    components/movies/movie-row.tsx
    components/movies/home-movies.tsx
    components/movies/discover-grid-content.tsx
    components/series/series-grid-content.tsx
    components/watchlist/watchlist-content.tsx
    components/ai/recommendations-grid.tsx
    components/movies/personalized-section.tsx
  </files>
  <action>
**1. Update `components/movies/movie-card.tsx`:**
- Add optional `href?: string` prop to `MovieCardProps`
- When `href` is provided: wrap the card in `<Link href={href}>` from `next/link` and do NOT call `onClick`
- When `href` is NOT provided (falsy): keep existing `onClick` behavior (for search drawer use)
- The card itself should remain a `motion.div` — wrap the `Link` around it or make the card's root element a `Link` when href is set
- Keep all existing functionality: poster, rating badge, watchlist buttons (gated by readOnly), hover effects

**2. Update `components/movies/movie-row.tsx`:**
- Add optional `mediaType?: "movie" | "tv"` prop (default: "movie")
- Remove `onMovieClick` prop (or make it optional with deprecation)
- Add `hrefPrefix?: string` prop that defaults to `/movie/` for movies and `/tv/` for TV
- Each `<MovieCard>` in the row gets `href={`${hrefPrefix}${movie.id}`}`
- For backward compatibility: if `onMovieClick` is passed and `hrefPrefix` is not, use the callback (search drawer case)

**3. Update `components/movies/home-movies.tsx`:**
- Remove modal state management (`selectedMovie`, `setSelectedMovie`)
- Remove `<MovieDetailModal>` render
- Pass `mediaType="movie"` (or just rely on default) to movie rows — cards will link to `/movie/{id}`
- The trending row, popular row, etc. all get link-based navigation
- Personalized rows: update `PersonalizedSection` to pass hrefs to its movie rows

**4. Update `components/movies/discover-grid-content.tsx`:**
- Movie cards in the grid: pass `href={`/movie/${movie.id}`}` to each MovieCard
- Search results: do NOT pass href — keep `onClick` that opens the search drawer (Task 2)
- Remove any remaining modal state from discover

**5. Update `components/series/series-grid-content.tsx`:**
- TV cards in the grid: pass `href={`/tv/${movie.id}`}` to each MovieCard (these are normalized TV shows)
- Keep `readOnly` prop on cards

**6. Update `components/watchlist/watchlist-content.tsx`:**
- Movie cards: pass `href={`/movie/${item.tmdbId}`}` to each card
- Remove modal state if it exists in the library page

**7. Update `components/ai/recommendations-grid.tsx`:**
- Movie cards: pass `href={`/movie/${movie.id}`}` to each card
- Remove modal state if it exists

**8. Update `components/movies/personalized-section.tsx`:**
- Pass link-based navigation to its movie rows
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit && npm run lint</automated>
    <manual>Click a movie card on /home — navigates to /movie/[id] detail page. Click a TV card on /series — navigates to /tv/[id]. Click a library item — navigates to /movie/[id].</manual>
  </verify>
  <done>All movie/TV cards across the app use link-based navigation to /movie/[id] or /tv/[id] detail pages. No modal opens when clicking a card from home, discover grid, series, or library.</done>
</task>

<task type="auto">
  <name>Task 2: Create search drawer, remove old navbar, final cleanup</name>
  <files>
    components/movies/movie-search-drawer.tsx
    components/movies/discover-grid-content.tsx
    components/layout/app-navbar.tsx
  </files>
  <action>
**1. Install shadcn Sheet component (if not already installed):**
```bash
npx shadcn@latest add sheet
```

**2. Create `components/movies/movie-search-drawer.tsx`:**
- "use client" component using shadcn `Sheet` (or `Drawer`) component
- Props: `{ movie: Movie | null, open: boolean, onOpenChange: (open: boolean) => void }`
- **Content** (when movie is set):
  - Backdrop image (if available) at top, small
  - Title, year, rating in a compact layout
  - Overview text (truncated to 3-4 lines)
  - Genre badges
  - "View Full Details" button/link → `<Link href={`/movie/${movie.id}`}>` that navigates to the detail page and closes the drawer
  - "Add to Library" quick action button (uses watchlist hooks)
- Sheet slides in from the right (or bottom on mobile) — `side="right"` on desktop, `side="bottom"` on mobile

**3. Wire search drawer into `discover-grid-content.tsx`:**
- When search is active, movie cards do NOT get `href` — they get `onClick` that sets `selectedSearchResult` state
- `<MovieSearchDrawer movie={selectedSearchResult} open={!!selectedSearchResult} onOpenChange={(open) => !open && setSelectedSearchResult(null)} />`
- Only search results use the drawer; grid results (from filters) use link navigation

**4. Delete `components/layout/app-navbar.tsx`:**
- Verify no remaining imports reference it (should be none after Plan 01 replaced it in layout)
- Delete the file
- If any other file still imports it, update that import

**5. Clean up unused modal imports:**
- If `MovieDetailModal` is no longer imported anywhere, consider keeping the file but adding a comment that it's preserved for reference. Do NOT delete it yet — it may be useful for the search drawer or future reuse.
- Remove any orphaned state management (selectedMovie, etc.) in components that were migrated
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build</automated>
    <manual>On /discover, type a search query → results appear in grid → click a search result → compact drawer slides in with movie info and "View Full Details" link → click link → navigates to /movie/[id]</manual>
  </verify>
  <done>Search results on Discover open a compact drawer with quick preview and "View Full Details" link. Old navbar component deleted. Build passes with zero errors. All features functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete UI/UX revamp</name>
  <files>None — verification only</files>
  <action>
Human visual verification of the complete Phase 7 UI/UX revamp. No code changes — this is a blocking checkpoint.

**What was built:** Complete UI/UX revamp: sidebar navigation, warmer color scheme, detail page routes, grid+filter browse pages, search drawer, and link-based card navigation.
  </action>
  <verify>
Verify all 10 areas:
1. **Sidebar (desktop):** Hover over left sidebar — expands from icon-only to full labels. Active item has crimson pill. Logo switches between icon and full text. User dropdown at bottom with logout.
2. **Bottom tab bar (mobile):** Resize to < 768px — sidebar hidden, bottom tab bar appears with 4 icons. Active item is crimson.
3. **Color scheme:** Background is warmer (slightly brownish-dark vs pure gray-dark).
4. **Movie detail page:** Navigate to any movie → `/movie/[id]` page with full-bleed backdrop, pill badges (runtime, year, rating), genre pills, cast chips, watch providers, "More Like This" row, fixed bottom action bar.
5. **TV detail page:** Navigate to any TV show → `/tv/[id]` page with seasons, episodes, status badge, "Created by:" label, no watchlist controls.
6. **Discover page:** Grid layout with Genre/Sort/Year filters. Change genre — grid updates. Scroll down — more movies load. Search — results appear, clicking opens drawer with "View Full Details" link.
7. **Series page:** Grid layout with TV Genre/Sort/Year filters. Cards are read-only. Clicking navigates to /tv/[id].
8. **Home page:** Hero banner, trending row, personalized rows, AI mood section — all working. Card clicks go to /movie/[id].
9. **Library page:** Cards click through to /movie/[id]. Tabs and status filters still work.
10. **AI mood section:** Still functional on /home. Recommendations grid cards link to /movie/[id].
  </verify>
  <done>All 10 feature areas verified by human. Phase 7 UI/UX revamp complete. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes with zero warnings
3. ALL 6 Phase 7 success criteria from ROADMAP are satisfied:
   - Top navbar replaced with collapsible sidebar (icon-only + expanded)
   - Color scheme updated with warmer tinted dark tones
   - Detail modal replaced with Stremio-inspired detail pages (pill metadata, action bar)
   - Discover and Series pages use grid layout with filter dropdowns
   - All existing features remain fully functional
   - Responsive: sidebar on desktop, bottom tab bar on mobile
4. No remaining references to `app-navbar.tsx`
5. Card clicks navigate to detail pages across all contexts (except Discover search → drawer)
</verification>

<success_criteria>
- Every movie/TV card in the app navigates to the appropriate detail page route
- Discover search results use the compact drawer with "View Full Details" link
- Old navbar component is deleted
- Human verification confirms all 10 feature areas are functional
- Clean build with zero errors and warnings
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-ux-revamp/07-04-SUMMARY.md`
</output>
