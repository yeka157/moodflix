---
phase: 07-ui-ux-revamp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/(app)/layout.tsx
  - components/layout/app-sidebar.tsx
  - components/layout/bottom-tab-bar.tsx
autonomous: true
requirements: [REVAMP-01]

must_haves:
  truths:
    - "Desktop users see a fixed left sidebar with 4 nav items (Home, Discover, Series, Library) instead of the top navbar"
    - "Sidebar shows icon-only (~60px) by default and expands to ~200px with labels on hover"
    - "Moodflix logo icon shows when collapsed, full logo text when expanded"
    - "User avatar with logout dropdown appears at the bottom of the sidebar"
    - "Mobile/tablet users see a fixed bottom tab bar with 4 icon buttons instead of the sidebar"
    - "Active nav item has a crimson highlight pill indicator"
    - "Background colors shift to warmer tinted dark tones (cinema feel)"
    - "Main content area has correct left padding for sidebar on desktop, bottom padding for tab bar on mobile"
  artifacts:
    - path: "components/layout/app-sidebar.tsx"
      provides: "Hover-expandable sidebar navigation"
      contains: "motion.aside"
    - path: "components/layout/bottom-tab-bar.tsx"
      provides: "Mobile bottom tab bar"
      contains: "md:hidden"
    - path: "app/(app)/layout.tsx"
      provides: "Updated layout with sidebar instead of navbar"
      contains: "AppSidebar"
    - path: "app/globals.css"
      provides: "Warmer dark color tokens"
      contains: "oklch(0.13 0.008 25)"
  key_links:
    - from: "app/(app)/layout.tsx"
      to: "components/layout/app-sidebar.tsx"
      via: "import and render"
      pattern: "AppSidebar"
    - from: "app/(app)/layout.tsx"
      to: "components/layout/bottom-tab-bar.tsx"
      via: "import and render"
      pattern: "BottomTabBar"
---

<objective>
Replace the top navbar with a hover-expandable sidebar (desktop) and bottom tab bar (mobile), and update the color scheme to warmer dark tones.

Purpose: This is the foundational layout change for the Phase 7 UI/UX revamp. All subsequent plans (detail pages, grid layouts) depend on this new layout structure being in place.
Output: New sidebar + bottom tab bar components, updated app layout, warmer color tokens in globals.css.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-revamp/07-RESEARCH.md

Key existing files:
@app/(app)/layout.tsx
@components/layout/app-navbar.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sidebar + bottom tab bar components and update color tokens</name>
  <files>
    components/layout/app-sidebar.tsx
    components/layout/bottom-tab-bar.tsx
    app/globals.css
  </files>
  <action>
**1. Create `components/layout/app-sidebar.tsx`:**
- "use client" component using Framer Motion `motion.aside` with `animate={{ width: expanded ? 200 : 60 }}`
- `position: fixed`, `top-0 left-0 h-full z-50`, hidden on mobile (`hidden md:flex`)
- Hover-to-expand: `onHoverStart` / `onHoverEnd` toggle `expanded` state (NO click/pin toggle)
- Spring transition: `{ type: "spring", stiffness: 280, damping: 26 }` with `useReducedMotion` gate (duration: 0 when reduced)
- Background: `bg-sidebar` (will be `oklch(0.11 0.008 25)` from color tokens), `border-r border-sidebar-border`
- Overflow hidden on the aside to clip labels during collapse
- **Logo area** (top, h-16, px-4): Show `MoodflixIcon` when collapsed, `MoodflixLogo` when expanded. Import from `@/components/brand/moodflix-logo` and `@/components/brand/moodflix-icon`. Use `AnimatePresence` or conditional render with opacity transition.
- **Nav items** (flex-1 flex-col gap-1 px-2): 4 links — Home (`/home`, Home icon, exact match), Discover (`/discover`, Compass icon), Series (`/series`, Tv icon), Library (`/library`, Bookmark icon). Use `usePathname()` for active detection. Active item: `text-primary` + animated pill background via `layoutId="sidebar-active-pill"` (gated by `useReducedMotion`). Inactive: `text-sidebar-foreground/60 hover:text-sidebar-foreground`. Each link is 44px min height (h-11). Labels use `motion.span` with `animate={{ opacity: expanded ? 1 : 0, width: expanded ? "auto" : 0 }}`.
- **User section** (bottom, px-2 pb-4): DropdownMenu from shadcn with user avatar (first letter of email in a circle) as trigger. Menu items: "Settings" (disabled, placeholder), separator, "Log out" (calls logout server action from `@/actions/auth`). When expanded, show email truncated beside avatar.
- Props: `{ user: { email: string } }`

**2. Create `components/layout/bottom-tab-bar.tsx`:**
- "use client" component, `position: fixed bottom-0 left-0 right-0 z-50`, `h-16`, shown only on mobile (`flex md:hidden`)
- Background: `bg-sidebar border-t border-sidebar-border`
- 4 flex-1 links (same nav items as sidebar), each with icon (size-5) + tiny label (text-[10px])
- Active detection via `usePathname()` — active icon gets `text-primary`, inactive gets `text-muted-foreground`
- Min touch target: 44px (flex-1 gives full width, h-16 is 64px height — satisfies 44px minimum)
- NO animation library needed — simple CSS color transitions

**3. Update `app/globals.css` color tokens:**
Update the `.dark` block with warmer tinted values (per user decision):
- `--background: oklch(0.13 0.008 25)` (was `oklch(0.13 0.004 25)`)
- `--card: oklch(0.16 0.01 25)` (was `oklch(0.16 0.006 25)`)
- `--muted: oklch(0.26 0.01 25)` (was `oklch(0.26 0.006 25)`)
- `--border: oklch(1 0.01 25 / 10%)` (was `oklch(1 0 0 / 10%)`)
- `--input: oklch(1 0.01 25 / 15%)` (was `oklch(1 0 0 / 15%)`)
- `--sidebar: oklch(0.11 0.008 25)` (was `oklch(0.16 0.006 25)`) — darker than page bg
- `--sidebar-border: oklch(1 0.01 25 / 10%)` (was `oklch(1 0 0 / 10%)`)
- `--secondary: oklch(0.22 0.01 25)` (was `oklch(0.22 0.006 25)`)
- `--popover: oklch(0.16 0.01 25)` (was `oklch(0.16 0.006 25)`)
- `--muted-foreground: oklch(0.65 0.01 25)` (no change — already warm)
Also update scrollbar-thumb to match: `oklch(0.35 0.01 25)` (same hue 25)
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit && npm run lint</automated>
    <manual>Check that sidebar renders on desktop with icon-only state, expands on hover; bottom tab bar shows on mobile viewport</manual>
  </verify>
  <done>Sidebar component and bottom tab bar component exist with all nav items, hover-expand behavior, user dropdown, and a11y motion support. Color tokens updated to warmer tinted dark palette.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sidebar into app layout, replace navbar</name>
  <files>
    app/(app)/layout.tsx
  </files>
  <action>
**Update `app/(app)/layout.tsx`:**
1. Replace `import { AppNavbar }` with `import { AppSidebar }` from `@/components/layout/app-sidebar` and `import { BottomTabBar }` from `@/components/layout/bottom-tab-bar`
2. Remove `<AppNavbar user={{ email: user.email ?? "" }} />`
3. Add `<AppSidebar user={{ email: user.email ?? "" }} />` and `<BottomTabBar />` before `<main>`
4. Update `<main>` classes: remove `pt-16` (was for top navbar), add `md:pl-[60px] pb-16 md:pb-0` (left padding for sidebar on desktop, bottom padding for tab bar on mobile)
5. Keep `NextTopLoader` — it sits at the very top of the viewport and won't conflict with sidebar (2px height)
6. Keep `Providers` wrapper and `container mx-auto px-4 py-8` for content

**Do NOT delete `app-navbar.tsx` yet** — it will be removed in Plan 04 after all references are confirmed gone. This prevents breaking anything during parallel development.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build</automated>
    <manual>Visit /home — sidebar visible on desktop left side, bottom tab bar on mobile, content correctly offset</manual>
  </verify>
  <done>App layout uses sidebar + bottom tab bar instead of top navbar. Content has correct offsets. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes with zero warnings
3. Desktop: sidebar visible at ~60px collapsed, expands to ~200px on hover, all 4 nav links work
4. Mobile (< 768px): bottom tab bar visible, sidebar hidden, all 4 nav links work
5. Active nav item shows crimson pill indicator
6. Background colors are warmer (visible shift from pure neutral to warm-tinted dark)
7. User dropdown shows at bottom of sidebar with logout option
</verification>

<success_criteria>
- Top navbar replaced with fixed left sidebar on desktop and fixed bottom tab bar on mobile
- Sidebar hover-expands from 60px to 200px with smooth spring animation
- Color scheme shifted to warmer tinted dark tones across all pages
- All existing routes (home, discover, series, library) accessible and functional
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-ux-revamp/07-01-SUMMARY.md`
</output>
