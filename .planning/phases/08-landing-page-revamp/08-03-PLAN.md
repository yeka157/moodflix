---
phase: 08-landing-page-revamp
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - components/landing/ai-preview-section.tsx
  - components/landing/cta-section.tsx
  - components/landing/footer.tsx
autonomous: true
requirements:
  - AI-DEMO-SECTION
  - CTA-CONVERSION
  - STORYTELLING-FLOW
must_haves:
  truths:
    - "AI preview section visually demonstrates the mood-to-recommendation flow"
    - "Final CTA section compels visitors to sign up with a clear value proposition"
    - "Footer matches the cinematic dark theme"
    - "All below-fold sections use whileInView scroll-triggered animations"
  artifacts:
    - path: "components/landing/ai-preview-section.tsx"
      provides: "AI mood discovery demo/preview section"
      contains: "whileInView"
    - path: "components/landing/cta-section.tsx"
      provides: "Final CTA section with signup link"
      contains: "signup"
    - path: "components/landing/footer.tsx"
      provides: "Landing page footer"
      contains: "Moodflix"
  key_links:
    - from: "components/landing/cta-section.tsx"
      to: "/signup"
      via: "Link href"
      pattern: "signup"
---

<objective>
Build the AI preview demo section, compelling final CTA, and footer — completing the landing page storytelling flow.

Purpose: The AI section is the unique differentiator that sets Moodflix apart. The CTA converts interest into signups. The footer provides closure.
Output: AI preview section, final CTA section, updated footer — all with scroll-triggered animations.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-page-revamp/08-RESEARCH.md
@.planning/phases/08-landing-page-revamp/08-01-SUMMARY.md
@.planning/phases/08-landing-page-revamp/08-02-SUMMARY.md
@components/landing/ai-preview-section.tsx
@components/landing/cta-section.tsx
@components/landing/footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI preview section with mood-to-recommendation visual</name>
  <files>
    components/landing/ai-preview-section.tsx
  </files>
  <action>
FULL REPLACE `components/landing/ai-preview-section.tsx`:

1. "use client" component. This section demonstrates the AI mood discovery feature — the app's unique differentiator.

2. Layout: Two-column on desktop (text left, visual right), stacked on mobile.
   - Left column: Section heading (e.g., "Tell us your mood. We'll find the movie."), compelling description of how it works (describe mood → AI recommends genres → browse results), bullet points or steps with icons
   - Right column: Mock UI showing the mood input experience:
     - A dark card (`bg-card border border-border rounded-2xl p-6`) simulating the chat interface
     - A fake user message bubble: "I'm feeling nostalgic and want something heartwarming"
     - A fake AI response with genre suggestion pills (e.g., "Drama", "Romance", "Family") in crimson accent bg
     - Style these to match the actual app's prompt-input/message UI
     - This is a static visual mockup — NOT interactive

3. Scroll animation:
   - Entire section fades up with `whileInView` + `viewport={{ once: true, amount: 0.2 }}`
   - Left text staggers in (heading, then description, then bullets)
   - Right mock UI card scales in slightly: `hidden: { opacity: 0, scale: 0.95 }` → `visible: { opacity: 1, scale: 1 }`
   - Use `"as const"` on all variants

4. Visual flair:
   - Subtle glow behind the mock UI card: `absolute -inset-4 bg-accent/5 blur-2xl rounded-3xl`
   - The genre pills should have a subtle shimmer or gradient effect

5. Reduced motion: skip scale/translate animations, show all content immediately.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Scroll to AI section — text staggers in on left, mock chat UI scales in on right. Genre pills visible with crimson accent.</manual>
  </verify>
  <done>AI preview section visually demonstrates the mood-to-recommendation flow with a mock chat UI, genre pills, and scroll-triggered animations. Two-column layout with cinematic styling.</done>
</task>

<task type="auto">
  <name>Task 2: Final CTA section + footer update</name>
  <files>
    components/landing/cta-section.tsx
    components/landing/footer.tsx
  </files>
  <action>
1. FULL REPLACE `components/landing/cta-section.tsx`:
   - "use client" component
   - Full-width section with generous padding (py-24 or more)
   - Centered content with max-w-3xl:
     - Compelling headline: "Ready to discover your next favorite movie?" or similar
     - Short subline reinforcing the value
     - Large crimson CTA button: "Get Started — It's Free" linking to /signup
     - Small text below: "No credit card required" or similar trust signal
   - Background: subtle radial gradient from accent/5 in center, or a faint backdrop pattern
   - Scroll animation: `whileInView` fade-up on the entire block, `viewport={{ once: true, amount: 0.3 }}`
   - Reduced motion support

2. UPDATE `components/landing/footer.tsx`:
   - Read current file first to understand structure
   - Update to match cinematic dark theme:
     - Dark bg matching the app: `bg-background border-t border-border/50`
     - MoodflixLogo (or MoodflixIcon + text) on the left
     - Simple links: About (placeholder #), Privacy (placeholder #), Terms (placeholder #)
     - Copyright: "© 2026 Moodflix. All rights reserved."
     - Small text: "Powered by TMDB" with TMDB attribution (required by TMDB terms)
   - No scroll animation needed on footer — it's the termination point
   - Minimal, clean design — don't over-design the footer
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build 2>&1 | tail -5</automated>
    <manual>Scroll to bottom — CTA section has compelling headline + crimson button linking to /signup. Footer shows logo, links, copyright, TMDB attribution.</manual>
  </verify>
  <done>CTA section has compelling copy, crimson signup button, and scroll animation. Footer matches dark theme with logo, links, copyright, and TMDB attribution.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. AI preview section demonstrates mood → genre flow with mock UI
3. CTA section has clear signup button and compelling copy
4. Footer shows TMDB attribution and matches app brand
5. All sections have scroll-triggered animations
</verification>

<success_criteria>
- AI section visually communicates the unique differentiator
- CTA provides clear conversion path to /signup
- Footer includes required TMDB attribution
- All below-fold sections animate on scroll
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-page-revamp/08-03-SUMMARY.md`
</output>
