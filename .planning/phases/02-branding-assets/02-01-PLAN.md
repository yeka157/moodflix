---
phase: 02-branding-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/brand/moodflix-icon.tsx
  - components/brand/moodflix-logo.tsx
  - components/brand/index.ts
  - app/layout.tsx
  - components/layout/app-navbar.tsx
  - components/landing/landing-navbar.tsx
  - components/auth/login-form.tsx
  - components/auth/signup-form.tsx
autonomous: false

must_haves:
  truths:
    - "Moodflix logo appears in the app navbar — wordmark on md+, M icon on mobile"
    - "Moodflix logo appears in the landing page navbar"
    - "Moodflix logo appears above the login and signup forms (replacing the plain text link)"
    - "The M icon has film-strip perforations (4 dark rect overlays on the left vertical stroke)"
    - "The wordmark renders in Bebas Neue (the M is crimson #FB2C36, 'oodflix' is white)"
    - "npm run build completes with zero TypeScript errors"
  artifacts:
    - path: "components/brand/moodflix-icon.tsx"
      provides: "Standalone M icon SVG component with film-strip perforations"
      exports: ["MoodflixIcon"]
    - path: "components/brand/moodflix-logo.tsx"
      provides: "Full wordmark SVG (M icon + 'oodflix' in Bebas Neue)"
      exports: ["MoodflixLogo"]
    - path: "components/brand/index.ts"
      provides: "Barrel export for brand components"
      contains: "export.*MoodflixIcon.*MoodflixLogo"
    - path: "app/layout.tsx"
      provides: "Bebas Neue font variable loaded as --font-display"
      contains: "Bebas_Neue"
  key_links:
    - from: "app/layout.tsx"
      to: "components/brand/moodflix-logo.tsx"
      via: "--font-display CSS variable consumed by SVG text fontFamily"
      pattern: "bebasNeue\\.variable"
    - from: "components/layout/app-navbar.tsx"
      to: "components/brand"
      via: "MoodflixLogo (desktop) and MoodflixIcon (mobile) in logo slot"
      pattern: "MoodflixLogo|MoodflixIcon"
    - from: "components/auth/login-form.tsx"
      to: "components/brand/moodflix-logo.tsx"
      via: "MoodflixLogo replaces the plain text Link in CardHeader"
      pattern: "MoodflixLogo"
---

<objective>
Create the Moodflix SVG brand component system (M icon + full wordmark), load Bebas Neue via next/font/google, and wire the logo into all placement points: app navbar, landing navbar, and auth forms.

Purpose: Establish the branded visual identity across every user-facing surface. The film-strip M icon is the centerpiece of the brand — crimson, geometric, and cinematic.
Output: 3 new brand component files + 5 updated files. Logo visible in navbar, landing, and auth pages.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-branding-assets/02-CONTEXT.md
@.planning/phases/02-branding-assets/02-RESEARCH.md
@components/layout/app-navbar.tsx
@components/landing/landing-navbar.tsx
@components/auth/login-form.tsx
@components/auth/signup-form.tsx
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MoodflixIcon and MoodflixLogo SVG components</name>
  <files>
    components/brand/moodflix-icon.tsx
    components/brand/moodflix-logo.tsx
    components/brand/index.ts
  </files>
  <action>
Create the `components/brand/` directory with three files.

**`components/brand/moodflix-icon.tsx`** — standalone M icon, pure SVG geometry, no font dependency:

```tsx
interface MoodflixIconProps {
  size?: number;
  variant?: "dark" | "light";
  className?: string;
}

export function MoodflixIcon({ size = 32, variant = "dark", className }: MoodflixIconProps) {
  const crimson = "#FB2C36";
  // cutoutColor must match the background the icon is placed on
  // dark variant (app): rects are #0a0a0a (app background)
  // light variant (press): rects are #ffffff
  const cutoutColor = variant === "dark" ? "#0a0a0a" : "#ffffff";
  const letterColor = variant === "dark" ? crimson : "#1a1a1a";

  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 100 100"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      className={className}
      aria-hidden="true"
    >
      {/* Geometric sans M — two vertical strokes + diagonals meeting at center peak */}
      <path
        d="M8 88 L8 12 L50 58 L92 12 L92 88 L80 88 L80 32 L50 70 L20 32 L20 88 Z"
        fill={letterColor}
      />
      {/* Film-strip perforations — 4 dark rects on the left vertical stroke (x: 8–20) */}
      {/* These overlay the crimson M path, visually punching holes through the left stroke */}
      <rect x="10" y="18" width="8" height="10" rx="1.5" fill={cutoutColor} />
      <rect x="10" y="34" width="8" height="10" rx="1.5" fill={cutoutColor} />
      <rect x="10" y="50" width="8" height="10" rx="1.5" fill={cutoutColor} />
      <rect x="10" y="66" width="8" height="10" rx="1.5" fill={cutoutColor} />
    </svg>
  );
}
```

**`components/brand/moodflix-logo.tsx`** — full wordmark (M icon geometry + "oodflix" in Bebas Neue via SVG `<text>`):

```tsx
interface MoodflixLogoProps {
  height?: number;
  variant?: "dark" | "light";
  showTagline?: boolean;
  className?: string;
}

export function MoodflixLogo({
  height = 32,
  variant = "dark",
  showTagline = false,
  className,
}: MoodflixLogoProps) {
  const crimson = "#FB2C36";
  const cutoutColor = variant === "dark" ? "#0a0a0a" : "#ffffff";
  const textColor = variant === "dark" ? "#ffffff" : "#1a1a1a";
  const letterColor = variant === "dark" ? crimson : "#1a1a1a";

  // ViewBox is 500x100. Width prop is derived from height to maintain 5:1 aspect ratio.
  const width = height * 5;

  return (
    <svg
      width={width}
      height={height}
      viewBox="0 0 500 100"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      className={className}
      role="img"
      aria-label="Moodflix"
    >
      {/* M icon (same geometry as MoodflixIcon, coordinates 0-100 of the 500-wide viewBox) */}
      <path
        d="M8 88 L8 12 L50 58 L92 12 L92 88 L80 88 L80 32 L50 70 L20 32 L20 88 Z"
        fill={letterColor}
      />
      <rect x="10" y="18" width="8" height="10" rx="1.5" fill={cutoutColor} />
      <rect x="10" y="34" width="8" height="10" rx="1.5" fill={cutoutColor} />
      <rect x="10" y="50" width="8" height="10" rx="1.5" fill={cutoutColor} />
      <rect x="10" y="66" width="8" height="10" rx="1.5" fill={cutoutColor} />

      {/* "oodflix" text — Bebas Neue via CSS variable --font-display */}
      {/* x=104 starts just after the M's right edge (x=92) with a small gap */}
      <text
        x="104"
        y="82"
        fontSize="78"
        fontWeight="400"
        fill={textColor}
        fontFamily="var(--font-display), sans-serif"
        letterSpacing="-1"
      >
        oodflix
      </text>

      {/* Optional tagline — landing page hero use only, not in navbar */}
      {showTagline && (
        <text
          x="104"
          y="98"
          fontSize="11"
          fill={textColor}
          opacity={0.55}
          fontFamily="var(--font-inter), sans-serif"
          letterSpacing="3.5"
        >
          AI MOVIE DISCOVERY
        </text>
      )}
    </svg>
  );
}
```

**`components/brand/index.ts`** — barrel export:

```ts
export { MoodflixIcon } from "./moodflix-icon";
export { MoodflixLogo } from "./moodflix-logo";
```

Important constraints:
- Do NOT use `oklch()` in any SVG `fill` attribute — use `#FB2C36` hex only
- The M path uses the same coordinates in both files — keep them identical
- `cutoutColor` must match the actual background surface (not transparent) — perforations are rect overlays, not true SVG cutouts
- `fontFamily="var(--font-display), sans-serif"` in the `<text>` element is correct React SVG syntax
  </action>
  <verify>
Run `npm run build` — must complete with zero TypeScript errors. Also run `npm run lint` — must pass.

Then open `http://localhost:3000` (after `npm run dev`) and visually check that the brand components can be imported without runtime errors. The components will be visible after Task 3 wires them in.
  </verify>
  <done>
All three brand component files exist. `MoodflixIcon` exports a 100x100 viewBox SVG with the M path + 4 perforation rects. `MoodflixLogo` exports a 500x100 viewBox SVG with M + "oodflix" SVG text. Barrel export in index.ts. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Load Bebas Neue font in layout.tsx</name>
  <files>
    app/layout.tsx
  </files>
  <action>
Update `app/layout.tsx` to load Bebas Neue via `next/font/google` and expose it as `--font-display`:

1. Add `Bebas_Neue` to the existing import from `"next/font/google"`:
   ```ts
   import { Inter, Geist_Mono, Bebas_Neue } from "next/font/google";
   ```

2. Add the font configuration after the existing `inter` and `geistMono` declarations:
   ```ts
   const bebasNeue = Bebas_Neue({
     weight: "400",          // ONLY valid weight — Bebas Neue is a single-weight font
     variable: "--font-display",
     subsets: ["latin"],
     display: "block",       // "block" prevents FOUT during logo render
   });
   ```

3. Add `bebasNeue.variable` to the `<body>` `className`:
   ```tsx
   className={`${inter.variable} ${geistMono.variable} ${bebasNeue.variable} font-sans antialiased`}
   ```

Do NOT change any other part of `layout.tsx` in this task — the metadata.icons and openGraph blocks are updated in Plan 02-02 and 02-03 respectively.

Critical: weight MUST be `"400"` — not `"700"` or `700`. Bebas Neue only ships one weight file (Regular). Specifying any other weight will cause a runtime error or silent fallback.
  </action>
  <verify>
Run `npm run build`. Confirm zero errors. Run `npm run dev`, open `http://localhost:3000`, and inspect the `<body>` element in DevTools — it should have a class containing `__variable` for Bebas Neue. Open the Network tab and confirm a `BebasNeue` woff2 font is fetched (or served from Next.js self-hosted cache).
  </verify>
  <done>
`Bebas_Neue` is imported and configured with `weight: "400"`, `variable: "--font-display"`, `display: "block"`. The body className includes the `bebasNeue.variable`. Build passes cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire logo into all placement points</name>
  <files>
    components/layout/app-navbar.tsx
    components/landing/landing-navbar.tsx
    components/auth/login-form.tsx
    components/auth/signup-form.tsx
  </files>
  <action>
Replace all text-based logo placeholders with the SVG brand components.

**`components/layout/app-navbar.tsx`:**

Add imports at the top:
```ts
import { MoodflixLogo } from "@/components/brand/moodflix-logo";
import { MoodflixIcon } from "@/components/brand/moodflix-icon";
```

Replace the existing logo `<Link>` block (currently lines 66-72 — the Link containing `<span>Mood</span><span>flix</span>`):
```tsx
<Link
  href="/home"
  className="flex items-center hover:opacity-80 transition-opacity"
  aria-label="Moodflix home"
>
  {/* Full wordmark on md+, M icon on mobile */}
  <span className="hidden md:flex">
    <MoodflixLogo height={28} variant="dark" />
  </span>
  <span className="flex md:hidden">
    <MoodflixIcon size={32} variant="dark" />
  </span>
</Link>
```

Remove the old logo Link. Keep all other navbar content unchanged.

**`components/landing/landing-navbar.tsx`:**

Add import:
```ts
import { MoodflixLogo } from "@/components/brand/moodflix-logo";
```

Replace the existing logo `<Link>` block (currently the Link with `<span>Mood</span><span>flix</span>`):
```tsx
<Link
  href="/"
  className="flex items-center hover:opacity-80 transition-opacity"
  aria-label="Moodflix"
>
  <MoodflixLogo height={30} variant="dark" />
</Link>
```

**`components/auth/login-form.tsx`:**

Add import (place after existing imports, before the `type` import):
```ts
import { MoodflixLogo } from "@/components/brand/moodflix-logo";
```

In `LoginFormInner`, inside `CardHeader`, replace the existing logo `<Link>` (currently the Link with `<span>Mood</span><span>flix</span>` at approximately line 113-119):
```tsx
<Link
  href="/"
  className="inline-flex items-center justify-center hover:opacity-80 transition-opacity"
  aria-label="Moodflix"
>
  <MoodflixLogo height={36} variant="dark" />
</Link>
```

**`components/auth/signup-form.tsx`:**

Apply the identical change as `login-form.tsx` — same import, same replacement of the CardHeader logo Link (approximately line 123-129 in signup-form.tsx).

**Hover behavior:** All four placements use `hover:opacity-80 transition-opacity` — subtle opacity, no complex animation.

**Accessibility:** Each Link gets an `aria-label="Moodflix"` since the SVG uses `aria-label` internally and the link itself needs a label for screen readers (to announce "Moodflix, link").
  </action>
  <verify>
Run `npm run build` and `npm run lint`. Both must pass with zero errors.

Run `npm run dev`, then:
1. Visit `http://localhost:3000` — logo should appear in the landing navbar (left side, wordmark ~150px wide)
2. Visit `http://localhost:3000/login` — logo should appear above the form inside the card header
3. Visit `http://localhost:3000/signup` — logo should appear above the form inside the card header
4. Visit `http://localhost:3000/home` (logged in) — logo should appear in the app navbar (desktop: wordmark, mobile: M icon at less than 768px)
5. Resize to mobile width (360px) — app navbar should show only the M icon, not the wordmark
6. Hover any logo — should see subtle opacity reduction (opacity-80)
  </verify>
  <done>
All four files updated. Logo renders correctly at all placements. App navbar shows wordmark on desktop, M icon on mobile. Landing and auth forms show wordmark. Hover shows opacity transition. Build and lint pass cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of brand logo placements</name>
  <action>Human verifies the SVG logo renders correctly at all placement points before proceeding to favicon generation.</action>
  <what-built>
    - MoodflixIcon: standalone M SVG with crimson letterform (#FB2C36) + 4 film-strip perforation rects (dark overlay technique)
    - MoodflixLogo: full wordmark SVG — crimson M + white "oodflix" text in Bebas Neue (via --font-display CSS variable)
    - Bebas Neue loaded in layout.tsx as --font-display, weight "400"
    - Logo wired into: app navbar (desktop wordmark / mobile icon), landing navbar, login form, signup form
  </what-built>
  <how-to-verify>
Run `npm run dev` and check each placement:

1. Landing page (`http://localhost:3000`): The navbar shows the Moodflix wordmark on the left. The M is crimson, "oodflix" is white in a bold condensed font (Bebas Neue). The M has 4 small rectangular holes cut into its left vertical stroke (the film-strip effect).

2. Login page (`http://localhost:3000/login`): The card header shows the Moodflix wordmark above "Welcome back".

3. Signup page (`http://localhost:3000/signup`): The card header shows the Moodflix wordmark above "Create your account".

4. App navbar (logged in, desktop `http://localhost:3000/home`): The left side shows the wordmark. On mobile width (less than 768px), it shows only the M icon (no text).

5. Hover any logo link — subtle opacity reduction (80%) on hover.

6. Check the M carefully: the left vertical stroke should have 4 small rectangular notches (film-strip holes). If they appear as solid dark shapes on top of the M (not actual through-holes), the cutout color (#0a0a0a) is matching the dark background correctly — this is expected behavior.

If the font has not loaded yet (first render), the "oodflix" may briefly appear in sans-serif before snapping to Bebas Neue — acceptable with display:block. After initial load it renders immediately in Bebas Neue.
  </how-to-verify>
  <verify>All logo placements render the branded SVG wordmark/icon correctly.</verify>
  <done>User has approved the visual appearance of all logo placements.</done>
  <resume-signal>Type "approved" to continue to Plan 02-02 (favicon generation), or describe any visual issues to fix first.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes with zero TypeScript errors
- `npm run lint` passes with zero warnings
- `components/brand/moodflix-icon.tsx` exists and exports `MoodflixIcon`
- `components/brand/moodflix-logo.tsx` exists and exports `MoodflixLogo`
- `components/brand/index.ts` exports both components
- `app/layout.tsx` imports `Bebas_Neue` with `weight: "400"` and `variable: "--font-display"`
- `app/layout.tsx` body className includes `bebasNeue.variable`
- All 4 placement files updated with SVG logo components
- No `oklch()` used in any SVG fill attribute (all use `#FB2C36` hex)
</verification>

<success_criteria>
The Moodflix SVG logo system is live. Users visiting any page of the app see the branded logo with the film-strip M in crimson. The wordmark renders in Bebas Neue at all logo placements. The build compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-branding-assets/02-01-SUMMARY.md` using the summary template.
</output>
