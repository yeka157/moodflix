---
phase: 07-ui-ux-revamp
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/(app)/discover/page.tsx
  - components/movies/discover-grid-content.tsx
  - app/(app)/series/page.tsx
  - components/series/series-grid-content.tsx
  - hooks/use-movies.ts
  - hooks/use-tv.ts
autonomous: true
requirements: [REVAMP-01]

must_haves:
  truths:
    - "Discover page shows a responsive grid of movies with filter dropdowns (Genre, Sort by, Year) instead of horizontal scroll rows"
    - "Series page shows a responsive grid of TV shows with the same filter dropdown pattern"
    - "Filters update the grid results dynamically via API calls"
    - "Infinite scroll loads more results as the user scrolls down"
    - "Home page layout remains unchanged (hero + rows + AI mood + feature cards)"
  artifacts:
    - path: "components/movies/discover-grid-content.tsx"
      provides: "Grid layout with filter dropdowns for movies"
      contains: "Select"
    - path: "components/series/series-grid-content.tsx"
      provides: "Grid layout with filter dropdowns for TV shows"
      contains: "Select"
    - path: "app/(app)/discover/page.tsx"
      provides: "Updated discover page using grid content"
    - path: "app/(app)/series/page.tsx"
      provides: "Updated series page using grid content"
  key_links:
    - from: "components/movies/discover-grid-content.tsx"
      to: "hooks/use-movies.ts"
      via: "useDiscoverByGenre or new discover hook"
      pattern: "useDiscover"
    - from: "components/series/series-grid-content.tsx"
      to: "hooks/use-tv.ts"
      via: "TV discover hook"
      pattern: "useDiscoverTV"
---

<objective>
Convert Discover and Series pages from horizontal scroll rows to grid layouts with filter dropdowns (Genre, Sort by, Year/decade) and infinite scroll.

Purpose: The grid+filter pattern matches the Revamp-UI-2.png reference and provides a more browsable, filterable experience for content discovery. Both pages adopt a consistent layout.
Output: New grid content components with shadcn Select filters, updated page routes, and new/updated TanStack Query hooks for filtered discovery.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-revamp/07-RESEARCH.md
@.planning/phases/07-ui-ux-revamp/07-01-SUMMARY.md

Key existing files:
@components/movies/discover-content.tsx (current horizontal row layout — reference)
@components/series/series-content.tsx (current horizontal row layout — reference)
@components/movies/movie-grid.tsx (existing grid component with infinite scroll)
@hooks/use-movies.ts (useDiscoverByGenre, useTrendingMovies, etc.)
@hooks/use-tv.ts (useTrendingTV, useKoreanDramas, etc.)
@hooks/use-infinite-scroll.ts (IntersectionObserver hook)
@lib/constants.ts (GENRES, TV_GENRES)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Select, create discover grid content with filters</name>
  <files>
    components/movies/discover-grid-content.tsx
    hooks/use-movies.ts
    app/(app)/discover/page.tsx
  </files>
  <action>
**0. Install shadcn Select component:**
```bash
npx shadcn@latest add select
```

**1. Add `useDiscoverMovies` hook to `hooks/use-movies.ts`:**
- New hook that wraps the existing `/api/movies?action=genre` endpoint (or extend it)
- Parameters: `{ genreId?: number, sortBy?: string, year?: number, page?: number }`
- The TMDB discover endpoint supports `sort_by`, `with_genres`, `primary_release_year` params
- Extend the existing `/api/movies` route handler to accept `sort_by` and `year` query params (pass through to TMDB discover)
- Return type matches existing `useDiscoverByGenre` pattern — `Movie[]` with pagination
- Use `useInfiniteQuery` for infinite scroll (existing pattern from `useDiscoverByGenre`)
- Deduplicate results by `movie.id` using `Set` in `useMemo` (existing gotcha)

**If the existing `/api/movies` route needs updating** to support `sort_by` and `year` params, update `app/api/movies/route.ts` as well:
- When `action=genre`, also read `sort_by` and `year` from searchParams
- Pass them to the TMDB discover call

**2. Create `components/movies/discover-grid-content.tsx`** ("use client"):
- State: `genreId` (string, default ""), `sortBy` (string, default "popularity.desc"), `year` (string, default "")
- **Filter bar** at top: three `<Select>` dropdowns in a flex row, responsive (stack on mobile):
  - **Genre**: Options from `GENRES` constant (All, Action, Comedy, Drama, etc.). "All" = empty string (no genre filter)
  - **Sort by**: "Most Popular" (popularity.desc), "Highest Rated" (vote_average.desc), "Newest" (primary_release_date.desc), "A-Z" (title.asc)
  - **Year**: "All Years" + decades (2020s, 2010s, 2000s, 1990s, 1980s) + specific recent years (2026, 2025, 2024, 2023, 2022, 2021, 2020). Decades translate to year range in the API call.
- **Search bar**: Keep the existing search input from current `discover-content.tsx`. Search results display inline in the grid (not a separate section). When search is active, hide filters (search overrides filter results).
- **Movie grid**: Use existing `<MovieGrid>` component or replicate its responsive grid classes: `grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6`
- **Infinite scroll**: Use `useInfiniteScroll` hook with sentinel div at bottom (existing pattern)
- Movie cards: For now, keep `onClick` behavior (modal) — Plan 04 migrates to link-based navigation
- **Empty state**: "No movies found for these filters" with suggestion to adjust filters

**3. Update `app/(app)/discover/page.tsx`:**
- Replace import of `DiscoverContent` with `DiscoverGridContent`
- Keep SSR structure (server component fetching initial data if needed, or let client handle all fetching)
- Simplify: since the grid is fully client-driven with filters, the page can just render `<DiscoverGridContent />` (no SSR prefetch needed — the client hooks handle it)
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit && npm run lint</automated>
    <manual>Visit /discover — see a grid of movie posters with Genre, Sort, and Year dropdowns. Changing Genre shows filtered results. Scrolling down loads more.</manual>
  </verify>
  <done>Discover page shows a filterable movie grid with Genre, Sort by, and Year dropdowns. Infinite scroll loads more results. Search bar still works. Filters reset when search is active.</done>
</task>

<task type="auto">
  <name>Task 2: Create series grid content with filters</name>
  <files>
    components/series/series-grid-content.tsx
    hooks/use-tv.ts
    app/(app)/series/page.tsx
    app/api/tv/route.ts
  </files>
  <action>
**1. Add `useDiscoverTV` hook to `hooks/use-tv.ts`:**
- New hook wrapping `/api/tv` endpoint with genre, sort, and year filters
- Extend `/api/tv/route.ts` to support a `discover` action (or `action=genre`) with `genre_id`, `sort_by`, `first_air_date_year` params
- Add a new TMDB fetch function in `lib/tmdb.ts` if needed: `discoverTV({ genreId, sortBy, year, page })` — calls TMDB `/discover/tv`
- Use `useInfiniteQuery` for infinite scroll, deduplicate by `id`
- Returns normalized `Movie[]` via `normalizeTVShow`

**2. Create `components/series/series-grid-content.tsx`** ("use client"):
- Same filter pattern as discover grid: Genre (using `TV_GENRES`), Sort by, Year dropdowns
- Same grid layout: `grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6`
- Infinite scroll with sentinel
- TV cards are `readOnly` (no watchlist buttons) — pass `readOnly` to each MovieCard
- No search bar on Series page (search stays on Discover only, per user decision)
- Keep the hero banner from current series page? Per CONTEXT.md: Home page keeps current layout, but Discover/Series switch to grid. Remove the series hero banner — the grid is the primary view now.

**3. Update `app/(app)/series/page.tsx`:**
- Replace current `SeriesContent` import with `SeriesGridContent`
- Simplify to client-driven approach (same as discover)
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build</automated>
    <manual>Visit /series — see a grid of TV show posters with Genre, Sort, and Year dropdowns. No watchlist buttons on cards. Infinite scroll works.</manual>
  </verify>
  <done>Series page shows a filterable TV show grid with Genre (TV_GENRES), Sort by, and Year dropdowns. Cards are read-only. Infinite scroll works. Build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `/discover` shows movie grid with 3 filter dropdowns, infinite scroll works
3. `/series` shows TV grid with 3 filter dropdowns (TV genres), infinite scroll works
4. Changing any filter reloads the grid with filtered results
5. TV cards on /series have no watchlist buttons (readOnly)
6. Search on /discover still works and shows results in the grid
7. `/home` page layout is unchanged (hero + rows)
8. Grid columns are responsive: 2 cols mobile, scaling up to 6 cols on xl
</verification>

<success_criteria>
- Discover and Series pages use grid layout with filter dropdowns instead of horizontal scroll rows
- Genre, Sort by, and Year filters work and update results dynamically
- Infinite scroll loads more content as user scrolls
- Existing search on Discover still functional
- Home page remains unchanged
- TV cards remain read-only (no watchlist controls)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-ux-revamp/07-03-SUMMARY.md`
</output>
