---
phase: 08-landing-page-revamp
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - app/page.tsx
  - components/landing/hero-section.tsx
autonomous: false
requirements:
  - VISUAL-QUALITY
  - ANIMATION-POLISH
  - STORYTELLING-FLOW
must_haves:
  truths:
    - "Landing page tells a coherent story from hero to CTA"
    - "All scroll animations are smooth and intentional"
    - "Page builds and passes lint with zero errors"
    - "Landing page is responsive at 375px, 768px, and 1280px"
    - "prefers-reduced-motion disables all motion effects"
  artifacts:
    - path: "app/page.tsx"
      provides: "Complete landing page composition with correct section order and server data"
      contains: "getHeroBackdrop"
  key_links:
    - from: "app/page.tsx"
      to: "all landing components"
      via: "import and render in storytelling order"
      pattern: "HeroSection|FeaturesSection|MovieShowcase|AiPreviewSection|CtaSection|Footer"
---

<objective>
Final integration pass: verify section ordering in app/page.tsx, run full build + lint, and visual verification checkpoint.

Purpose: Ensure all 4 plans integrate into a cohesive landing page with correct storytelling flow, no build errors, and polished responsive behavior.
Output: Verified, shippable landing page.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-page-revamp/08-01-SUMMARY.md
@.planning/phases/08-landing-page-revamp/08-02-SUMMARY.md
@.planning/phases/08-landing-page-revamp/08-03-SUMMARY.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration pass — section order, spacing, build verification</name>
  <files>
    app/page.tsx
    components/landing/hero-section.tsx
  </files>
  <action>
1. Read `app/page.tsx` and verify the section composition order matches the storytelling flow:
   - LandingNavbar (sticky top)
   - HeroSection (cinematic backdrop + screenshot + CTAs)
   - FeaturesSection (4 feature cards)
   - MovieShowcase (real poster marquee)
   - AiPreviewSection (AI mood demo)
   - CtaSection (final conversion CTA)
   - Footer

   If any sections are in a different order, reorder them. Add `id="features"` to FeaturesSection wrapper if not present (for hero "Learn More" scroll-to link).

2. Verify spacing between sections: each section should have consistent vertical padding (py-20 or py-24). Add spacer divs or section padding adjustments if sections feel too cramped or too spread.

3. Verify `app/page.tsx` exports proper metadata:
   - title: "Moodflix — Discover Movies That Match Your Mood"
   - description: compelling SEO description mentioning AI mood discovery, TMDB browse, watchlist

4. Run `npm run lint` — fix any ESLint issues in landing components.
5. Run `npm run build` — fix any TypeScript errors.
6. Quick check: ensure no `Math.random()` in any Server Component, no `any` types, no missing imports.
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run lint 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
    <manual>N/A — automated checks sufficient for this task</manual>
  </verify>
  <done>Landing page sections are in correct storytelling order, metadata is set, lint passes, build passes with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — full landing page</name>
  <what-built>
    Complete landing page revamp with:
    - Cinematic hero (TMDB backdrop + parallax + screenshot placeholder + CTAs)
    - 4-feature grid with scroll-triggered stagger animations
    - Real TMDB poster marquee (dual-direction)
    - AI mood discovery preview with mock chat UI
    - Final CTA section with signup button
    - Updated footer with TMDB attribution
    - Landing navbar with MoodflixLogo
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000 (logged out / incognito)
    3. **Hero section**: Verify movie backdrop image loads, parallax effect on scroll, headline/CTAs visible, screenshot placeholder present
    4. **Scroll down**: Verify content fades as you leave hero
    5. **Features section**: Verify 4 cards stagger-animate in (AI Mood, Browse, Library, TV Series)
    6. **Movie showcase**: Verify real movie posters scroll in two rows (left + right)
    7. **AI preview**: Verify mock chat UI with mood message and genre pills
    8. **CTA section**: Verify compelling copy and crimson signup button
    9. **Footer**: Verify logo, links, TMDB attribution
    10. **Responsive**: Resize to 375px width — verify mobile layout stacks properly
    11. **Reduced motion**: Enable prefers-reduced-motion in DevTools → verify no animations play
    12. **Navbar**: Verify MoodflixLogo, Sign In link, Get Started button are visible and linked correctly
  </how-to-verify>
  <resume-signal>Type "approved" to ship, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run lint` passes with zero warnings
2. `npm run build` passes with zero errors
3. All sections render in correct storytelling order
4. Human confirms visual quality at desktop and mobile breakpoints
</verification>

<success_criteria>
- Landing page is a cohesive, cinematic storytelling experience
- All animations are smooth, scroll-triggered, and respect reduced motion
- Build and lint pass cleanly
- Human approves the visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-page-revamp/08-04-SUMMARY.md`
</output>
