---
phase: 08-landing-page-revamp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/tmdb.ts
  - app/page.tsx
  - components/landing/hero-section.tsx
  - components/landing/landing-navbar.tsx
autonomous: true
requirements:
  - HERO-BACKDROP
  - HERO-SCREENSHOT
  - CINEMATIC-THEME
must_haves:
  truths:
    - "Hero section displays a real TMDB movie backdrop image with parallax scroll effect"
    - "App screenshot is visually layered on top of the backdrop in the hero"
    - "Hero headline and CTA communicate the app's value proposition immediately"
    - "Landing navbar shows MoodflixLogo and Sign In / Get Started links"
    - "Hero backdrop fades out as user scrolls down"
  artifacts:
    - path: "components/landing/hero-section.tsx"
      provides: "Cinematic parallax hero with backdrop + screenshot overlay"
      contains: "useScroll"
    - path: "lib/tmdb.ts"
      provides: "getHeroBackdrop server function"
      exports: ["getHeroBackdrop"]
    - path: "components/landing/landing-navbar.tsx"
      provides: "Landing page navbar with MoodflixLogo"
      contains: "MoodflixLogo"
  key_links:
    - from: "app/page.tsx"
      to: "lib/tmdb.ts"
      via: "getHeroBackdrop() call in Server Component"
      pattern: "getHeroBackdrop"
    - from: "app/page.tsx"
      to: "components/landing/hero-section.tsx"
      via: "backdropUrl prop passed from server to client"
      pattern: "backdropUrl"
---

<objective>
Build the cinematic hero section with real TMDB backdrop imagery, parallax scroll, app screenshot overlay, and updated landing navbar.

Purpose: The hero is the first thing visitors see — it must immediately communicate "this is a premium movie app" through cinematic visuals and real product proof.
Output: Working hero section with parallax backdrop, screenshot overlay, compelling headline/CTA, and aligned landing navbar.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-page-revamp/08-RESEARCH.md
@components/landing/hero-section.tsx
@components/landing/landing-navbar.tsx
@app/page.tsx
@lib/tmdb.ts
@components/ui/moodflix-logo.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero section with parallax backdrop + screenshot overlay</name>
  <files>
    lib/tmdb.ts
    app/page.tsx
    components/landing/hero-section.tsx
  </files>
  <action>
1. In `lib/tmdb.ts`, add a `getHeroBackdrop()` export:
   - Fetch `/trending/movie/week` using the existing `tmdbFetch` helper
   - Find the first movie with a non-null `backdrop_path`
   - Return the full URL using `w1280` size: `https://image.tmdb.org/t/p/w1280${backdrop_path}`
   - Return `null` on failure (try/catch). No `Math.random()` — deterministic first-match.
   - Use `{ next: { revalidate: 3600 } }` for 1-hour ISR cache

2. In `app/page.tsx` (Server Component), call `getHeroBackdrop()` and pass the result as `backdropUrl` prop to `<HeroSection>`. Keep all other component imports intact.

3. FULL REPLACE `components/landing/hero-section.tsx`:
   - "use client" component accepting `backdropUrl: string | null`
   - Use `useRef` + `useScroll({ target: heroRef, offset: ["start start", "end start"] })` for scroll tracking
   - `useTransform(scrollYProgress, [0, 1], ["0%", "25%"])` for backdrop parallax Y
   - `useTransform(scrollYProgress, [0, 0.6], [1, 0])` for content opacity fade-out
   - Structure:
     - `<section ref={heroRef} className="relative min-h-screen overflow-hidden flex items-center">`
     - Parallax backdrop layer: `motion.div` with `style={{ y: backdropY }}`, containing `next/image` with `fill priority` + dark gradient overlays (bottom gradient to `bg-background`, left-side vignette)
     - Fallback: if `backdropUrl` is null, render dark solid bg `bg-[oklch(0.11_0.008_25)]`
     - Content layer: `motion.div` with `style={{ opacity: contentOpacity }}` containing:
       - Small badge/chip: "AI-Powered Movie Discovery" (crimson accent bg)
       - Headline: "Discover movies that match your mood" (large, bold, Inter font)
       - Subline: 1-2 sentences about the app's value
       - Two CTA buttons: "Get Started" (crimson filled, links to /signup) + "Learn More" (ghost/outline, scrolls to features section)
     - App screenshot: Use a placeholder `<div>` with aspect-video and rounded-xl border styled as a browser frame mockup. Add comment `{/* TODO: Replace with real screenshot after capture */}`. Use CSS gradient to simulate a dark app screenshot placeholder. Position it offset to the right on desktop, below CTAs on mobile.
   - Respect `motion-safe:` / `motion-reduce:` — wrap parallax transforms in `useReducedMotion` check, fall back to static positioning
   - Responsive: stack vertically on mobile (content above, screenshot below), side-by-side on lg+
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visit localhost:3000 — hero shows a real movie backdrop with parallax on scroll, gradient overlays, headline, two CTA buttons, and a screenshot placeholder frame</manual>
  </verify>
  <done>Hero section renders with real TMDB backdrop, parallax scroll effect, content fade-on-scroll, compelling headline + dual CTAs, and screenshot placeholder frame. getHeroBackdrop exports from lib/tmdb.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Update landing navbar to match app identity</name>
  <files>
    components/landing/landing-navbar.tsx
    app/page.tsx
  </files>
  <action>
1. Read current `components/landing/landing-navbar.tsx` and update to align with the app's current brand:
   - Use `MoodflixLogo` component (import from `@/components/ui/moodflix-logo`) for the logo — same as the app sidebar uses
   - Navigation: "Sign In" text link (links to /login) + "Get Started" button (crimson filled, links to /signup)
   - Sticky top with backdrop blur: `fixed top-0 w-full z-50 bg-background/80 backdrop-blur-md border-b border-border/50`
   - Responsive: logo + buttons visible at all breakpoints, minimal padding on mobile
   - Add subtle entrance animation: `motion.nav` with fade-in-down on mount (`initial={{ y: -20, opacity: 0 }} animate={{ y: 0, opacity: 1 }}`)
   - Ensure hero section accounts for navbar height (add `pt-16` or similar to hero section if navbar is fixed)

2. Update `app/page.tsx` if needed to ensure component imports and prop passing are correct. The page must:
   - Import and render `LandingNavbar` at the top
   - Pass `backdropUrl` to `HeroSection`
   - Keep all other section components in order
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build 2>&1 | tail -5</automated>
    <manual>Landing page navbar shows MoodflixLogo on the left, Sign In + Get Started on the right, sticky with blur on scroll</manual>
  </verify>
  <done>Landing navbar uses MoodflixLogo, has Sign In link + Get Started button, is fixed/sticky with backdrop blur, and aligns with app's current visual identity.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no TypeScript errors
2. Landing page at localhost:3000 shows cinematic hero with real movie backdrop
3. Scrolling triggers parallax effect on backdrop and content fade
4. Navbar is sticky with MoodflixLogo and auth links
5. Hero has two CTA buttons linking to /signup and scrolling to features
</verification>

<success_criteria>
- Hero section displays a real TMDB backdrop with parallax scroll depth
- App screenshot placeholder is positioned as an overlay on the hero
- Landing navbar uses MoodflixLogo and matches app brand identity
- Content fades as user scrolls past the hero
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-page-revamp/08-01-SUMMARY.md`
</output>
