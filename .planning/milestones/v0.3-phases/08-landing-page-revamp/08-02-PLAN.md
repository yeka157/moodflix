---
phase: 08-landing-page-revamp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/landing/features-section.tsx
  - components/landing/movie-showcase.tsx
autonomous: true
requirements:
  - FEATURES-4-EQUAL
  - SCROLL-ANIMATIONS
must_haves:
  truths:
    - "All 4 core features (AI Mood Discovery, Movie & TV Browse, Personal Library, TV Series) are highlighted equally"
    - "Feature cards animate in with scroll-triggered stagger when section enters viewport"
    - "Movie showcase displays real TMDB poster images in an infinite marquee"
    - "Animations fire once per visit (viewport once: true)"
  artifacts:
    - path: "components/landing/features-section.tsx"
      provides: "4-feature grid with scroll-triggered stagger reveal"
      contains: "whileInView"
    - path: "components/landing/movie-showcase.tsx"
      provides: "Real TMDB poster marquee"
      contains: "marquee"
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/features-section.tsx"
      via: "Direct import in page composition"
      pattern: "FeaturesSection"
    - from: "app/page.tsx"
      to: "components/landing/movie-showcase.tsx"
      via: "posterUrls prop from server fetch"
      pattern: "posterUrls"
---

<objective>
Build the 4-feature highlights section with scroll-triggered stagger animations and upgrade the movie showcase marquee to use real TMDB posters.

Purpose: Features section proves the app's breadth (AI + browse + library + TV series). The poster marquee adds cinematic social proof with real movie imagery.
Output: Fully animated features grid (4 cards) and real-poster marquee — both with scroll-triggered reveals.
</objective>

<execution_context>
@/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-page-revamp/08-RESEARCH.md
@components/landing/features-section.tsx
@components/landing/movie-showcase.tsx
@app/page.tsx
@lib/tmdb.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4-feature highlights with scroll-triggered stagger</name>
  <files>
    components/landing/features-section.tsx
  </files>
  <action>
FULL REPLACE `components/landing/features-section.tsx`:

1. "use client" component. Define 4 features as a const array:
   - **AI Mood Discovery** — icon: Sparkles (lucide-react), description about describing your mood and getting AI recommendations
   - **Movie & TV Browse** — icon: Film (lucide-react), description about TMDB-powered discover with filters, genres, infinite scroll
   - **Personal Library** — icon: BookmarkCheck (lucide-react), description about watchlist with instant status updates and ratings
   - **TV Series** — icon: Tv (lucide-react), description about Korean/Chinese drama discovery and series detail pages

2. Section layout:
   - Section heading: "Everything you need" or similar compelling headline, centered, with a subline
   - The heading itself should animate in with `whileInView` fade-up
   - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6` inside `max-w-7xl mx-auto px-4`

3. Animation using Framer Motion variants (per RESEARCH.md Pattern 2):
   ```
   containerVariants: { hidden: {}, visible: { transition: { staggerChildren: 0.12, delayChildren: 0.1 } } }
   cardVariants: { hidden: { opacity: 0, y: 40 }, visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: [0.25, 0.1, 0.25, 1] } } }
   ```
   - Container: `motion.div` with `variants={containerVariants} initial="hidden" whileInView="visible" viewport={{ once: true, amount: 0.2 }}`
   - Each card: `motion.div` with `variants={cardVariants}`

4. Card design (cinematic dark theme):
   - Dark card bg: `bg-card/50 border border-border/50 rounded-2xl p-6`
   - Icon in a crimson-tinted circle: `bg-accent/10 text-accent rounded-xl w-12 h-12 flex items-center justify-center`
   - Feature name (bold, text-lg) + description (text-muted-foreground, text-sm)
   - Subtle hover: `hover:border-accent/30 transition-colors duration-300`

5. Use `"as const"` on variants for TypeScript strict mode compatibility.
6. Respect reduced motion: import `useReducedMotion` from framer-motion, if true set `initial="visible"` (skip animation).
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Scroll to features section — 4 cards stagger in from bottom when section enters viewport. All 4 features present with icons.</manual>
  </verify>
  <done>Features section shows all 4 core features in a responsive grid with scroll-triggered stagger animation. Cards have cinematic dark styling with crimson accent icons.</done>
</task>

<task type="auto">
  <name>Task 2: Movie showcase with real TMDB posters</name>
  <files>
    components/landing/movie-showcase.tsx
    app/page.tsx
    lib/tmdb.ts
  </files>
  <action>
1. In `lib/tmdb.ts`, add a `getShowcasePosters()` export:
   - Fetch `/trending/movie/week` using existing `tmdbFetch`
   - Extract first 10 movies that have a non-null `poster_path`
   - Return array of `{ title: string; posterUrl: string }` with posterUrl as `https://image.tmdb.org/t/p/w342${poster_path}`
   - Use `w342` size (not original) for fast loading in small marquee cards
   - Use `{ next: { revalidate: 3600 } }` for 1-hour ISR
   - Return empty array on failure

2. In `app/page.tsx`, call `getShowcasePosters()` and pass result as `posters` prop to `<MovieShowcase>`.

3. REPLACE `components/landing/movie-showcase.tsx`:
   - Accept `posters: Array<{ title: string; posterUrl: string }>` prop
   - If posters array is empty, hide the section entirely (return null)
   - Section heading: "Trending Now" or similar, centered, with whileInView fade-in
   - Two marquee rows (existing CSS `marquee-left` / `marquee-right` keyframes from globals.css):
     - Row 1: posters scrolling left (duplicated for seamless loop)
     - Row 2: posters scrolling right (duplicated for seamless loop)
   - Each poster: `next/image` with `width={160} height={240}` (or similar aspect-ratio-2/3), `rounded-lg`, subtle shadow
   - Wrap each poster in a card with hover scale effect: `hover:scale-105 transition-transform duration-300`
   - `overflow-hidden` on the marquee container to prevent horizontal scroll
   - Use `motion-reduce:` Tailwind variant to pause marquee animation: `motion-reduce:[animation-play-state:paused]`
   - Add a gradient mask on left/right edges for fade-out effect using CSS mask-image or overlaid gradient divs
  </action>
  <verify>
    <automated>cd /Users/kevin/Repository/moodflix && npm run build 2>&1 | tail -5</automated>
    <manual>Movie showcase section shows real movie poster images scrolling in two rows (left and right). Posters have hover scale. Edges fade out.</manual>
  </verify>
  <done>Movie showcase displays real TMDB trending posters in a dual-direction infinite marquee. Posters use next/image with proper sizing. Edge fade masks applied. getShowcasePosters exports from lib/tmdb.ts.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Features section shows exactly 4 features with scroll-triggered stagger
3. Movie showcase shows real TMDB poster images in infinite marquee
4. Animations respect prefers-reduced-motion
</verification>

<success_criteria>
- All 4 core features (AI Mood, Browse, Library, TV Series) are equally highlighted
- Feature cards animate in on scroll with stagger timing
- Movie showcase uses real TMDB poster images (not gradient placeholders)
- Dual-direction marquee scrolls smoothly with edge fade
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-page-revamp/08-02-SUMMARY.md`
</output>
